# Premier XI — คู่มือกลไกเกม (Game Mechanics Guide)

เอกสารนี้สรุป **กลไกเกม เศรษฐศาสตร์ (economy) และระบบสุ่มทั้งหมด** จากโค้ดจริง ณ วันที่ 2026-07-16
ใช้เป็นคู่มืออ่านทำความเข้าใจภาพรวมเกม (ต่างจาก `docs/system-reference.md` ที่เป็นเอกสารโครงสร้างโค้ดสำหรับนักพัฒนา)

> แหล่งอ้างอิงโค้ด: `src/lib/packs.ts`, `src/lib/daily.ts`, `src/lib/economy.ts`, `src/lib/starter.ts`, `src/lib/chemistry.ts`, `src/lib/chemistryConfig.ts`, `src/lib/cardgen.ts`, `prisma/schema.prisma`

---

## 1. สกุลเงินในเกม (Currencies)

| สกุลเงิน | field ใน DB | ใช้ทำอะไร | ได้จากไหน |
|---|---|---|---|
| **Silver** | `silver` | เปิด Standard Pack (300/ครั้ง) | Starter Pack, Daily login |
| **Gold** | `gold` | เปิด Evolution Pack (10/ครั้ง), Royal Prime Pack (20/ครั้ง) | Daily login (เล็กน้อย), เติมเงินจริง (mock deposit) |
| **Shards** (`shards`) | จากการ์ดซ้ำ tier Bronze/Silver/Gold | แลกเปิด Standard Pack ฟรี | เปิดซองได้การ์ดซ้ำใน pool normal |
| **Evo Shards** (`evoShards`) | จากการ์ดซ้ำ tier Hero | แลกเปิด Evolution Pack ฟรี | เปิดซองได้การ์ด Evolution ซ้ำ |
| **Prime Shards** (`primeShards`) | จากการ์ดซ้ำ tier Legend | แลกเปิด Royal Prime Pack ฟรี | เปิดซองได้การ์ด Royal Prime ซ้ำ |
| `packTicket` | เดิมใช้เปิด Ticket Pack | **ยกเลิกแล้ว** (2026-07-16) เก็บ field ไว้เฉยๆ ไม่มีทางได้เพิ่มอีก | - |

Shard ทั้ง 3 พูล **แยกกันเด็ดขาด** — shard จาก Standard Pack เอาไปแลก Evolution Pack ไม่ได้ (กันคนใช้ shard ถูกไปแลกซองแพง)

---

## 2. EXP และ Level

### แหล่งที่มาของ EXP

| กิจกรรม | EXP ที่ได้ | หมายเหตุ |
|---|---|---|
| เปิดซองใดก็ได้ (Standard/Evolution/Royal Prime) | **+20 ต่อการเปิด 1 ครั้ง** | คงที่ ไม่ว่าจะได้การ์ดกี่ใบ/tier อะไร (เปิดทีละ 5 ใบ = ยัง +20 เท่าเดิม) |
| เช็คอินรายวัน (Daily Claim) | **+30 ต่อวัน** | คงที่ทุกวัน ไม่ขึ้นกับ streak |
| Starter Pack (ครั้งแรก) | ไม่มี EXP | ให้แค่การ์ด 11 ใบ + Silver 300 |

### สูตรเลื่อนระดับ (Level-up formula)

```
while (exp >= level * 100) {
  exp -= level * 100
  level += 1
}
```

หมายความว่า EXP ที่ต้องใช้เพื่อเลื่อนจาก level ปัจจุบันไป level ถัดไป = **`level ปัจจุบัน × 100`**

| จาก Level | EXP ที่ต้องสะสม |
|---|---|
| 1 → 2 | 100 |
| 2 → 3 | 200 |
| 3 → 4 | 300 |
| 4 → 5 | 400 |
| N → N+1 | N × 100 |

ตัวอย่าง: level 1, exp 0 → เปิดซอง 5 ครั้ง (100 exp) = เลื่อนเป็น level 2 พอดี

โค้ด: `applyExp()` ใน `src/lib/economy.ts` (pure function ไม่แตะ DB) — `finalizeOpen` (`packs.ts`) และ `claimDaily` (`daily.ts`) เรียกร่วมกัน แทนที่จะก็อป while-loop ซ้ำแบบเดิม (เดิมมี 3 ชุดซ้ำกัน รวมถึง `grantExp()` ที่เป็น dead code — ลบทิ้งแล้ว)

### รางวัล Level-up — เพิ่ม 2026-07-16

ตาราง `levelReward()` ใน `src/lib/economy.ts` (เช็คจากเงื่อนไขสูงไปต่ำ ได้แค่ระดับเดียวต่อเลเวล กันซ้อนทับตอนหารลงตัวหลายเงื่อนไข เช่น level 25 หารด้วย 5 ลงตัวด้วย):

| เงื่อนไข | ได้ | เหตุผล |
|---|---|---|
| ทุกเลเวล | Silver = `level × 20` | ตาม gdd.txt "ทุก Level ได้ Silver" — เทียบกับรายได้ daily login (100-400+/วัน) เป็นแค่ topping เสริม ไม่ทำเงินเฟ้อ |
| ทุก 5 เลเวล (5,10,15,20...) | + Standard Pack ฟรี | milestone ระยะสั้น-กลาง ใช้กลไก `grantFreePack` เดิม (เหมือน login milestone) |
| ทุก 10 เลเวล (10,20,30...) | + Evolution Pack ฟรี + Gold 5 | milestone ระยะกลาง-ยาว Gold ให้น้อยตั้งใจ (เทียบ weekly trickle +5/สัปดาห์) |
| ทุก 25 เลเวล (25,50,75...) | + Royal Prime Pack ฟรี + Gold 10 | milestone ใหญ่ระยะยาวมาก (level 25 ต้องสะสม 30,000 EXP สะสม) สอดคล้องแนวทาง Royal Prime = completionist goal ระยะยาว |

Cosmetic ตาม gdd.txt ยังไม่ทำ (ยังไม่มีระบบรองรับในโค้ดเลย เป็น scope แยก)

แจ้งผ่าน notification `LEVEL_UP` เดิม (ไม่มี UI ใหม่) — `notifyLevelRewards()` ใน `src/lib/notifications.ts` สรุปยอด Silver/Gold รวม + รายชื่อซองที่ได้ทั้งหมดต่อการ claim/เปิดซองครั้งเดียว (รวมรางวัลจากหลายเลเวลถ้า EXP ก้อนใหญ่ข้ามหลายเลเวลพร้อมกัน)

---

## 3. ระบบซอง (Packs) — เปิดทีละ 5 ใบ/ครั้งเสมอ (`CARDS_PER_OPEN = 5`)

มี 3 ซอง ไม่มี Premium/Ticket/Pity แล้ว (ยกเลิกทั้งหมดตั้งแต่ 2026-07-16)

### 3.1 Standard Pack

| รายการ | ค่า |
|---|---|
| สกุลเงิน | Silver |
| ราคา | **300 Silver** |
| การันตี | ไม่มี — สุ่มอิสระ 5 ใบจาก pool `normal` (นักเตะพรีเมียร์ลีก OVR ≤ 90) |

**อัตราสุ่ม tier ต่อใบ (fillerRates):** — ปรับ 2026-07-16 (ดูเหตุผลในหัวข้อ 13.2)

| Tier | % | เดิม (ก่อนปรับ) |
|---|---|---|
| Bronze | 25% | 55% |
| Silver | 50% | 38% |
| Gold | 25% | 7% |

### 3.2 Evolution Pack

| รายการ | ค่า |
|---|---|
| สกุลเงิน | Gold |
| ราคา | **10 Gold** |
| การันตี | 1 ใบจากพูล Evolution (tier **Hero**, OVR 90-92, มี 44 ใบ) แบบสุ่ม uniform 1/44 |
| โบนัสใบที่ 2 | **10%** โอกาสได้การ์ด Evolution เพิ่มอีก 1 ใบ (จากพูลเดียวกัน) |
| ใบที่เหลือ (3-4 ใบ) | สุ่มจากพูล normal ด้วยอัตรา Bronze 10% / Silver 50% / Gold 40% |

### 3.3 Royal Prime Pack

| รายการ | ค่า |
|---|---|
| สกุลเงิน | Gold |
| ราคา | **20 Gold** |
| การันตี | 1 ใบจากพูล Royal Prime (tier **Legend**, OVR 92-98, มี 44 ใบ) แบบสุ่ม uniform 1/44 |
| โบนัสใบที่ 2 | **12%** โอกาสได้การ์ด Royal Prime เพิ่มอีก 1 ใบ (จากพูลเดียวกัน) |
| ใบที่เหลือ (3-4 ใบ) | สุ่มจากพูล normal ด้วยอัตรา Bronze 10% / Silver 50% / Gold 40% |

> **หมายเหตุ:** การันตีของ Evolution/Royal Prime สุ่มแบบ uniform ล้วนๆ (1/44 ต่อใบ) — **ไม่มีระบบ pity** กันซ้ำติดกัน ดวงล้วนๆ

### สรุปตารางซองทั้งหมด

| ID | ชื่อ | สกุลเงิน | ราคา | Bronze | Silver | Gold | การันตีพิเศษ |
|---|---|---|---|---|---|---|---|
| `standard` | Standard Pack | Silver | 300 | 25% | 50% | 25% | - |
| `evolution` | Evolution Pack | Gold | 10 | 10% | 50% | 40% | Hero การันตี 1 (+10% ใบที่ 2) |
| `royalprime` | Royal Prime Pack | Gold | 20 | 10% | 50% | 40% | Legend การันตี 1 (+12% ใบที่ 2) |

---

## 4. ระบบ Shard (การ์ดซ้ำ)

ถือการ์ดได้ **1 ใบต่อ 1 การ์ดเท่านั้น** — ถ้าสุ่มได้การ์ดที่มีอยู่แล้ว จะไม่ได้การ์ดซ้ำ แต่แปลงเป็น Shard แทนตาม tier:

| Tier | Shard ที่ได้ | ลงพูลไหน |
|---|---|---|
| Bronze | 5 | `shards` |
| Silver | 15 | `shards` |
| Gold | 50 | `shards` |
| Hero (Evolution) | 100 | `evoShards` |
| Legend (Royal Prime) | 250 | `primeShards` |

### แลก Shard เป็นซองฟรี (Shard Exchange)

| แลกจาก | Cost | ได้ | คิดเป็นจำนวนการ์ดซ้ำ tier เดียวกัน |
|---|---|---|---|
| `shards` | 500 | Standard Pack ฟรี | 100 ใบ Bronze ซ้ำ หรือ 33 ใบ Silver ซ้ำ หรือ 10 ใบ Gold ซ้ำ |
| `evoShards` | 500 | Evolution Pack ฟรี | 5 ใบ Hero ซ้ำ |
| `primeShards` | 1,000 | Royal Prime Pack ฟรี | 4 ใบ Legend ซ้ำ |

> ต้นทุน `shards` ปรับจาก 600 → 500 เมื่อ 2026-07-16 เพื่อลดช่องว่าง rebate ระหว่างซอง (ดูด้านล่าง)

### อัตราคืนกลับ (rebate) เมื่อคอลเลกชันเต็มแล้ว

| ซอง | Shard เฉลี่ยต่อการเปิด 1 ครั้ง | ต้องเปิดกี่ครั้งถึงได้ฟรี 1 ครั้ง | rebate |
|---|---|---|---|
| Standard | 106.25 | ~4.7 | **~21.3%** |
| Evolution | 110 | ~4.6 | **~22%** |
| Royal Prime | 280 | ~3.6 | **~28%** |

เดิม Standard rebate ~10% (ห่างจาก Evolution/Royal Prime มาก) — ปรับต้นทุนแลกจาก 600 → 500 shard ให้ใกล้เคียงกันมากขึ้น (Royal Prime ยังสูงกว่าเล็กน้อยเพราะราคาซองแพงกว่ามาก ถือว่ายอมรับได้)

---

## 5. Daily Login (เช็คอินรายวัน)

รางวัลคำนวณจาก `day = ((streak - 1) % 7) + 1` (วนรอบ 7 วัน) — **streak ขาด 1 วัน = รีเซ็ตกลับวันที่ 1**

### สูตรรางวัลต่อวัน

| ส่วนประกอบ | สูตร |
|---|---|
| Silver | `100 + day × 30` (+300 พิเศษถ้า day = 7) |
| EXP | 30 คงที่ทุกวัน |
| Gold | +5 ถ้า day = 7 (ทุกสัปดาห์ — ปรับจาก +2 เมื่อ 2026-07-16), +5 เพิ่มถ้า streak หาร 30 ลงตัว (ทุก 30 วัน) |

### ตารางตัวอย่างรางวัล (7 วันแรก)

| Day | Silver | EXP | Gold |
|---|---|---|---|
| 1 | 130 | 30 | 0 |
| 2 | 160 | 30 | 0 |
| 3 | 190 | 30 | 0 |
| 4 | 220 | 30 | 0 |
| 5 | 250 | 30 | 0 |
| 6 | 280 | 30 | 0 |
| 7 | 610 | 30 | 5 |

รวม 7 วันแรก (login ต่อเนื่อง): **1,840 Silver, 210 EXP, 5 Gold**

ถ้า login ต่อเนื่องครบ 30 วัน (ไม่ขาดเลย): รวม **~7,650 Silver, 900 EXP, 25 Gold** (4 ครั้ง day-7 ได้ Gold ×5 = 20, บวก day-30 bonus +5)

> **หมายเหตุสำคัญ:** bonus ทุก 30 วัน (+5) ต้อง login **ติดต่อกันไม่ขาดเลยสักวัน** — พลาดวันเดียว streak รีเซ็ตเป็น 1 ทันที บอนัสนี้จึงพึ่งพาไม่ได้จริงสำหรับผู้เล่นทั่วไป รายได้ Gold ที่มั่นคงคือ weekly trickle (+5/สัปดาห์) เท่านั้น

> Silver ที่ได้ต่อวันถูก calibrate ให้เปิด Standard Pack (300 Silver) ได้ประมาณ 1 ครั้งทุก 1-1.5 วันถ้า login ต่อเนื่อง

---

## 6. Login Milestone (โปรโมชั่นเปิดตัวเกม — Launch Promotion)

นับจาก **`totalLogins`** (จำนวนวันที่เคย login รวมทั้งหมด — **ไม่รีเซ็ตแม้ streak ขาด** ต่างจาก daily streak) ไม่ใช่ streak ต่อเนื่อง

| Milestone | เงื่อนไข | รางวัล | ซ้ำได้ไหม |
|---|---|---|---|
| Evolution free pack | login สะสมครบ **15 วัน** | Evolution Pack ฟรี **1 ซอง = 5 ใบ** (เปิดทันทีอัตโนมัติ ได้ Hero การันตี 1 ใบตามปกติ) | ครั้งเดียวตลอดไป |
| Royal Prime free pack | login สะสมครบ **30 วัน** | Royal Prime Pack ฟรี **1 ซอง = 5 ใบ** (เปิดทันทีอัตโนมัติ ได้ Legend การันตี 1 ใบตามปกติ) | ครั้งเดียวตลอดไป |

ปัจจุบัน**ไม่มีวันหมดเขต** — เป็น mechanic ถาวรของเกม ไม่ใช่โปรโมชั่นช่วงเวลาจำกัด (แม้ชื่อจะเรียกว่า launch promotion)

ชนได้ **1 milestone ต่อการเคลม 1 ครั้งเท่านั้น** (เพราะ totalLogins เพิ่มทีละ 1)

---

## 7. Starter Pack (ผู้เล่นใหม่ — เปิดได้ครั้งเดียว)

| รายการ | ค่า |
|---|---|
| จำนวนการ์ด | **11 ใบ** — ตรงตำแหน่งเป๊ะตาม formation 4-3-3 (GK, LB, CB×2, RB, CM×3, LW, ST, RW) |
| Silver แถม | **300** |
| Tier การ์ด | 9 ใบ Bronze/Silver (OVR < 75) แบบสุ่ม + **2 ใบ Gold การันตี** (OVR 75-78 แคบกว่า Gold เต็ม tier เพื่อไม่ให้แรงเกินสมดุล) |
| ตำแหน่ง Gold 2 ใบ | สุ่มว่าเป็นตำแหน่งไหนใน 11 ตำแหน่ง |

ผลลัพธ์: ผู้เล่นใหม่จัดทีมเต็ม 11 ตำแหน่งได้ทันทีโดยไม่ต้องรอเปิดซองเพิ่ม

> **หมายเหตุ:** การ์ดตรงตำแหน่ง **ไม่ได้แปลว่า chemistry เต็ม** — ตรงตำแหน่งให้แค่ position factor ×1.0 ส่วนคะแนนฐานมาจาก link (สโมสร/ชาติ เท่านั้น หลังตัด league ออกแล้ว — ดูหัวข้อ 10) ทีม starter ที่สุ่มมาคนละสโมสรคนละชาติ (ไม่มี synergy เลย) จะได้ **0/33** ไม่ใช่ 33/33 (เดิมก่อนตัด league เคยได้ floor ~22/33 อัตโนมัติจาก league เดียวกันหมด — ดู 13.1)
>
> **จุดเปราะในโค้ด:** ถ้าตำแหน่งไหนไม่มีการ์ด Gold OVR 75-78 เลย โค้ดจะ fallback ไปหยิบการ์ด OVR<75 แทนโดยไม่ไปหา Gold จากตำแหน่งอื่นมาชดเชย (`src/lib/starter.ts:87-105`) — ข้อมูลปัจจุบันทุกตำแหน่งมี Gold 75-78 อยู่ (น้อยสุดคือ LW/RW มีตำแหน่งละ 1 ใบ) การันตี 2 ใบจึงยังเป็นจริง แต่ถ้าลบการ์ด LW/RW Gold ออกเมื่อไร การันตีจะเงียบ ๆ หายไป

---

## 8. เติมเงินจริง (Mock Deposit)

| รายการ | ค่า |
|---|---|
| อัตราแลกเปลี่ยน | **100 บาท = 10 Gold** (`DEPOSIT_RATE_GOLD_PER_BAHT = 0.1`) |
| First Deposit Bonus | **+20%** Gold พิเศษ เฉพาะการเติมเงินจริง**ครั้งแรก**เท่านั้น (เช็คจาก `User.hasDeposited`) |

ตัวอย่าง: เติม 500 บาทครั้งแรก = 50 Gold + โบนัส 10 Gold (20%) = **60 Gold รวม**
เติมครั้งถัดไป 500 บาท = 50 Gold (ไม่มีโบนัสแล้ว)

> **คำเตือน:** `mockDeposit()` เป็น internal helper ที่ **เสก Gold ขึ้นมาโดยไม่มีการตรวจสอบการชำระเงินใด ๆ** ปัจจุบันยังปลอดภัยเพราะไม่มี server action ห่อไว้ (client เรียกไม่ถึง) — **ห้ามสร้าง server action ห่อฟังก์ชันนี้ตรง ๆ เด็ดขาด** ต้องมี payment verification ก่อนเสมอ ไม่ใช่ "backend พร้อมแล้ว" อย่างที่เข้าใจกัน

---

## 9. การ์ดในเกม (Card Pools)

| Category | Tier | จำนวนใบ | ช่วง OVR | ที่มา |
|---|---|---|---|---|
| `normal` | Bronze | 40 | ต่ำกว่า 65 | 20 ทีมพรีเมียร์ลีก |
| `normal` | Silver | 213 | 65-74 | 20 ทีมพรีเมียร์ลีก |
| `normal` | Gold | 313 | 75-90 | 20 ทีมพรีเมียร์ลีก |
| `evolution` | **Hero** | 44 | 90-92 | นักเตะระดับพิเศษ (Evolution pool) |
| `royalprime` | **Legend** | 44 | 92-98 | นักเตะระดับสูงสุด (Royal Prime pool) |

รวมทั้งหมด **654 ใบ** (566 normal + 44 Evolution + 44 Royal Prime)

### Tier ของการ์ด normal คำนวณจาก OVR อัตโนมัติ (`deriveTier`)

| OVR | Tier |
|---|---|
| ≥ 75 | Gold |
| 65-74 | Silver |
| < 65 | Bronze |

### ค่าพลัง 6 ตัว (pace/shooting/passing/dribbling/defending/physical)

คำนวณ**อัตโนมัติแบบ deterministic**จาก OVR + กลุ่มตำแหน่ง (ไม่มีค่าสุ่ม) — ผู้เล่นตำแหน่งเดียวกัน OVR เท่ากัน จะมีค่าพลังเท่ากันเป๊ะ

| กลุ่มตำแหน่ง | pace | shooting | passing | dribbling | defending | physical |
|---|---|---|---|---|---|---|
| GK | OVR-18 | OVR-45 | OVR-8 | OVR-25 | OVR-30 | OVR-2 |
| DEF | OVR-2 | OVR-25 | OVR-8 | OVR-8 | OVR+6 | OVR+4 |
| MID | OVR-3 | OVR-5 | OVR+5 | OVR+4 | OVR-4 | OVR-2 |
| ATT | OVR+5 | OVR+7 | OVR-5 | OVR+6 | OVR-30 | OVR-4 |

(ค่าทุกตัวถูก clamp ไว้ระหว่าง 30-99)

---

## 10. Chemistry & Team Rating (จัดทีม)

Chemistry เต็มทีม = **33 แต้ม** (11 ตำแหน่ง × สูงสุด 3 แต้ม/คน)

### วิธีคำนวณต่อผู้เล่น 1 คน

1. รวมคะแนน "link" กับเพื่อนร่วมทีมคนอื่นในทีม:
   - อยู่สโมสรเดียวกัน: **+2** ต่อคู่
   - ชาติเดียวกัน: **+1** ต่อคู่
   - ~~ลีกเดียวกัน: +0.5 ต่อคู่~~ **ตัดออกแล้ว 2026-07-16** เพราะเกมนี้มีแต่ Premier League ลีกเดียว การ์ดทุกใบแมตช์กัน 100% เสมอ ทำให้กลายเป็น floor ปลอมที่การันตี teamChem อย่างน้อย 22/33 (67%) โดยไม่ต้องจัดทีมเก่งอะไรเลย (ยืนยันด้วยจำลองจริง) หลังตัดออก range เต็ม 0-33 มีความหมายกับการจัดทีมจริงแล้ว — ดูหัวข้อ 13.2
2. แปลง link score รวม → คะแนนฐาน (base) 0-3:

| Link score รวม | Base |
|---|---|
| ≥ 9 | 3 |
| ≥ 5 | 2 |
| ≥ 2 | 1 |
| < 2 | 0 |

3. คูณด้วย position factor ตามตำแหน่งที่ลงเล่นจริงในสนาม:

| ลงตำแหน่ง | Factor |
|---|---|
| ตรงตำแหน่งหลัก/ตำแหน่งรอง (altPositions) | ×1.0 |
| กลุ่มเดียวกัน (เช่น CM ลง CDM) | ×0.6 |
| คนละกลุ่ม (เช่น DEF ลง ATT) | ×0.3 |

### OVR Penalty ถ้าเล่นผิดตำแหน่ง — เพิ่ม 2026-07-16

นอกจาก chemistry แล้ว OVR ที่ใช้คิด `avgOvr` จะถูกหักลบถ้าเล่นผิดตำแหน่ง (แยกคนละกลไกกับ chemistry point ข้างบน):

| ลงตำแหน่ง | OVR Penalty |
|---|---|
| ตรงตำแหน่งหลัก/ตำแหน่งรอง (altPositions) | ไม่หัก |
| กลุ่มเดียวกัน (เช่น CB ลง LB) | **-10** |
| คนละกลุ่ม (เช่น GK ลง ST) | **-25** |

> **ทำไมต้องมี:** เดิม rating มีแต่โบนัสจาก chemistry ไม่มีบทลงโทษเลย ทำให้กลยุทธ์ที่ดีที่สุดคือ "ยัดการ์ด OVR สูงสุดทุกช่องไม่สนตำแหน่ง" เสมอ (ทดสอบจริง: การ์ด OVR 98 ยัดทุกช่องโดยไม่สนตำแหน่งเคยได้ rating ชนะทีมที่จัดถูกตำแหน่งขาดลอย) การเพิ่ม penalty ทำให้ตำแหน่งมีความหมายจริง — กลยุทธ์ที่ดีที่สุดตอนนี้คือใช้การ์ดพิเศษเฉพาะตำแหน่งที่มีจริง แล้วเติมช่องที่ขาด (เช่น LB/RB ที่ Royal Prime pool ไม่มีเลย) ด้วยการ์ด Gold ตรงตำแหน่ง แทนที่จะฝืนยัดการ์ดพิเศษผิดตำแหน่งลงไป

### Team Rating

```
avgOvr = เฉลี่ยของ (OVR ผู้เล่น - OVR Penalty ตามตำแหน่ง) ของทั้งทีม
rating = round(avgOvr × (1 + (teamChem / 33) × 0.1))
```

Chemistry เต็ม 33/33 → โบนัส rating สูงสุด **+10%** จาก avgOvr (หลังหัก penalty แล้ว)

---

## 11. Formation ที่เลือกได้

| Formation | โครงสร้าง |
|---|---|
| 4-3-3 (default) | GK, LB, CB×2, RB, CM×3, LW, ST, RW |
| 4-4-2 | GK, LB, CB×2, RB, LM, CM×2, RM, ST×2 |
| 3-5-2 | GK, CB×3, LM, CM×3, RM, ST×2 |
| 4-2-3-1 | GK, LB, CB×2, RB, CDM×2, CAM, LW, RW, ST |

---

## 12. สรุปสิ่งที่ยกเลิกไปแล้ว (อย่าอ้างอิงระบบเหล่านี้)

| ระบบเดิม | สถานะ |
|---|---|
| Premium Pack | ยกเลิก 2026-07-16 |
| Ticket Pack + `packTicket` currency | ยกเลิก 2026-07-16 (field ยังอยู่ในฐานข้อมูลแต่ไม่แจกเพิ่มแล้ว) |
| Pity Counter (การันตี Gold ทุก 10 ครั้ง) | ยกเลิก 2026-07-16 (field `pityCounter` ยังอยู่แต่ไม่ใช้แล้ว) |

---

## 13. ผลรีวิวระบบ (2026-07-16) — ปัญหาที่รู้แล้วแต่ยังไม่แก้

รีวิวโดย Codex + ทดสอบจริงด้วยสคริปต์ยิง concurrent request

### 13.1 บั๊ค/ช่องโหว่ (ยืนยันด้วยการทดสอบจริง)

| ระดับ | เรื่อง | รายละเอียด |
|---|---|---|
| **Critical** | `resetTestUserAction` / `devLoginAction` เรียกได้โดยไม่ต้อง login | `src/app/actions/auth.ts:81-118` — เป็น server action ที่ export ออกมาโดยไม่เช็ค auth และไม่เช็ค `NODE_ENV` ใครก็ยิงได้ตรง ๆ เพื่อ **ลบบัญชี `test` ทิ้งทั้งบัญชี** (cascade ลบการ์ด/ทีม/แจ้งเตือนหมด) หรือเข้าเป็น user `test` ต้องถอดออกก่อนขึ้น production |
| **High** | เปิดซองพร้อมกัน 5 ครั้งขึ้นไป = ล้มหมด (DB deadlock) | ทดสอบจริง: concurrent 5 → **สำเร็จ 0/5** ค้าง 5,034ms ครบ transaction timeout; concurrent 10 → สำเร็จแค่ 1/10 สาเหตุ: Prisma เปิด connection pool หลายเส้นไปหา SQLite แล้ว transaction แย่ง write lock กัน → deadlock **ข้อมูลไม่เสียหาย** (rollback ถูกต้อง เงินไม่หาย ไม่ได้การ์ดฟรี) แต่ผู้เล่น 5 คนกดพร้อมกัน = พังทั้งหมด และ user คนเดียวเขียนสคริปต์ยิงรัวก็ล็อกฐานข้อมูลได้ |
| **High** | *(วิธีแก้ที่ทดสอบแล้วได้ผล)* | เติม `?connection_limit=1` ต่อท้าย `DATABASE_URL` → concurrent 5 สำเร็จ **5/5 ใน 34ms**, concurrent 10 สำเร็จ **10/10 ใน 66ms** (เร็วขึ้น ~150 เท่า) |
| **Medium** | หักเงินแบบ "อ่านก่อน แล้วค่อย decrement" | `src/lib/packs.ts:225-235`, `src/lib/economy.ts:44-58` — ไม่มีเงื่อนไขระดับ DB (`WHERE silver >= 300`) และ schema ไม่มี `CHECK (silver >= 0)` ตอนนี้ยังไม่รั่วเพราะ SQLite ล็อกให้ แต่ถ้าย้ายไป Postgres เมื่อไรจะเกิด overspend ทันที ควรเปลี่ยนเป็น `updateMany({ where:{ id, silver:{ gte: cost }}})` แล้วเช็ค `count === 1` |
| **Low** | วันเปลี่ยนตอน 07:00 น. ไทย | `src/lib/daily.ts:5-7` ใช้ UTC dayIndex ไม่ใช่เวลาไทย ผู้เล่นเคลม 06:50 แล้วเคลมอีกทีตอน 07:10 ได้ 2 ครั้งใน "วันไทย" เดียวกัน (ยังเป็นคนละ UTC day จึงไม่ใช่การโกง แต่ถ้าสื่อว่าเป็นวันไทยคือไม่ตรง) |

**สิ่งที่ทดสอบแล้วว่า ปลอดภัย** — เคลม daily / เปิดซอง / Starter Pack / milestone ยิงพร้อมกัน 10 ครั้ง ไม่เกิดรางวัลซ้ำ ไม่เกิดเงินติดลบ, server action หลักดึง `userId` จาก session ไม่รับจาก client, `packId`/`exchangeId` ถูก whitelist ด้วย object lookup, เวลาใช้ของ server ล้วน — **แก้เวลาเครื่องตัวเองโกง daily ไม่ได้**

### 13.2 Balance — จุดที่ควรพิจารณา

| เรื่อง | ตัวเลข | ความเห็น |
|---|---|---|
| **~~การ์ด Gold แทบไม่มีทางเก็บครบ~~ — แก้แล้ว 2026-07-16** | เดิม Gold มี 313 ใบ (55% ของ pool) แต่สุ่มเจอแค่ 7% → เก็บครบต้องเปิด ~5,660 ซอง **ปรับ Standard fillerRates เป็น Bronze 25% / Silver 50% / Gold 25%** (เดิม 55/38/7) → โอกาสได้ Gold อย่างน้อย 1 ใบ/ซอง จาก 30.4% เป็น **76.3%**, เก็บครบ 313 ใบเฉลี่ยเหลือ **~1,584 ซอง** | ผลข้างเคียงที่ดี: rebate ของ Standard shard exchange ขยับจาก ~10% เป็น **~17.7%** (ใกล้ Evolution 22%/Royal Prime 28% มากขึ้น) โดยลำดับความคุ้มค่ายังถูกต้อง (Standard 25% Gold < ซองพรีเมียม filler 40% Gold) — อ้างอิง `src/lib/packs.ts:33-34` |
| **Bronze ซ้ำเร็วมาก** | เดิม pool 40 ใบ เจอ 55% (เฉลี่ย 2.75 ใบ/ซอง) — การปรับ Gold rate ข้างบนลด Bronze ลงเหลือ 25% ไปด้วย (เฉลี่ย 1.25 ใบ/ซอง) ช่วยลดความถี่การซ้ำไปในตัว | ยังเป็น pool เล็กที่สุด ถ้าต้องการให้ซ้ำช้าลงอีกต้องเพิ่มจำนวนการ์ด Bronze ในอนาคต |
| **~~F2P เข้าถึง Gold ช้ามาก~~ — แก้แล้ว 2026-07-16** | เดิม weekly Gold trickle +2/สัปดาห์ → พึ่ง weekly ล้วน (ไม่นับ bonus 30 วันที่พลาดวันเดียวก็หาย) ต้องรอถึงวันที่ 35 ถึงเปิด Evolution ได้ และวันที่ 70 ถึงเปิด Royal Prime ได้ **ปรับเป็น +5/สัปดาห์** (`src/lib/daily.ts:34`) → เหลือวันที่ **14** (Evolution) และวันที่ **28** (Royal Prime) | หลังหมด milestone ฟรี (วัน 15/30) ผู้เล่นที่ login สม่ำเสมอเปิด Evolution ได้ทุก ~2 สัปดาห์ และ Royal Prime ได้ทุก ~4 สัปดาห์ด้วยเงินฟรีล้วน ยังช้ากว่าคนจ่ายเงินมาก (100 บาท = 10 Gold ทันที) เพื่อไม่ตัดแรงจูงใจจ่ายเงิน |
| **Royal Prime เก็บครบยากมาก — ตัดสินใจแล้ว: ไม่แก้** | pool 44 ใบ, ได้ 1.12 Legend/ซอง → ต้อง ~171 ซอง = **3,420 Gold ≈ 34,200 บาท** | ไม่ใช่บั๊ค เป็น completionist goal ระยะยาวแบบตั้งใจ (เหมือนเกมการ์ดทั่วไป) — **คงไว้เหมือนเดิม ไม่แก้** |
| **~~ทีมแรงสุดซื้อได้ถูก~~ — แก้แล้ว 2026-07-16** | สาเหตุจริงไม่ใช่ราคา Royal Prime แต่เป็นสูตร rating เดิมที่ **ไม่มีบทลงโทษเลยถ้าเล่นผิดตำแหน่ง** มีแต่โบนัส (ทดสอบจริง: ก่อนแก้ ยัดการ์ด OVR98 ทุกช่องไม่สนตำแหน่งเคยชนะทีมที่จัดถูกตำแหน่งขาดลอย) พบด้วยว่า Royal Prime pool **ไม่มีการ์ดตำแหน่ง LB/RB เลยสักใบ** (Evolution มี RB แค่ 1 ใบ) | **เพิ่ม OVR Penalty ตามตำแหน่ง** ใน `src/lib/chemistry.ts`/`chemistryConfig.ts`: กลุ่มเดียวกัน -10, คนละกลุ่ม -25 (แยกจาก chemistry bonus เดิม) ทดสอบแล้ว: ยัดการ์ด OVR98 ไม่สนตำแหน่งเหลือ avgOvr 78 (แพ้ทีม Gold ตรงตำแหน่งที่ได้ 87) — กลยุทธ์ที่ดีที่สุดตอนนี้คือใช้การ์ดพิเศษเฉพาะตำแหน่งที่มีจริง + เติม Gold ตรงตำแหน่งในช่องที่ special ไม่มี (เช่น LB/RB) **ไม่ได้เพิ่มการ์ด LB/RB ใหม่** เพราะต้องใช้ asset รูปการ์ดที่ยังไม่มี ถ้าจะเพิ่มต้องแยกเป็นงาน content ทีหลัง |
| **~~rebate ยิ่งซองแพงยิ่งได้เปรียบ~~ — แก้แล้ว 2026-07-16** | เดิม Standard ~10% ห่างจาก Evolution ~22%/Royal Prime ~28% มาก | ลด `SHARD_EXCHANGE.standard.cost` จาก 600 → **500** (`src/lib/packs.ts:61`) → Standard rebate ขยับเป็น **~21.3%** ใกล้เคียง Evolution มาก โดยไม่ต้องแตะซองอื่น |
| **~~Chemistry แทบไม่มีผล~~ — แก้แล้ว 2026-07-16** | ทดสอบจริง (ก่อนแก้): ทีมแย่สุดที่จัดได้ = 22/33 (เพราะทุกการ์ดเป็น Premier League หมด → league link ให้ base chemistry ฟรี) ดีสุด 33/33 ต่างกันแค่ rating 85 vs 88 → OVR ชนะทุกอย่างเสมอ | นอกจาก OVR Penalty ข้างบน (แก้ "เล่นผิดตำแหน่งไม่มีต้นทุน") ยัง **ตัด league link ออกจากสูตร chemistry ทั้งหมด** (ยืนยันกับทีมแล้วว่าเกมนี้มีแต่ Premier League ลีกเดียวตลอดไป ไม่มีแผนเพิ่มลีกอื่น) เหลือแค่ club (+2) / nation (+1) ทดสอบจริงหลังแก้: worst-case (ไม่มี synergy เลย) = 0/33 (เดิม 22/33), best-case = 33/33 เท่าเดิม, ทีมผสมทั่วไป ~14/33 (+4.2%) — ได้ range เต็ม 0-33 ที่มีความหมายกับการจัดทีมจริง `MAX_CHEM_RATING_BONUS` (0.10) **ตัดสินใจคงค่าเดิมไว้** เพราะที่ avgOVR 80: worst→best ต่างกัน +8 OVR ถือว่ามีน้ำหนักสมเหตุสมผลแล้วหลังแก้ floor bug (ที่เคย "รู้สึกต่ำ" เป็นเพราะ floor บีบ range แคบ ไม่ใช่ cap ต่ำจริง) |
| **rating ทะลุ 99 ได้** | ทีม Legend OVR 95 chem 22/33 → rating **101** | ไม่มี clamp ที่ `src/lib/chemistry.ts:83-85` |
| **~~EXP ชันแบบกำลังสอง แต่ไม่มีรางวัล~~ — แก้แล้ว 2026-07-16** | สะสม EXP ถึง level N ≈ `50N(N-1)` · 30 วันแรก ≈ Lv.5 · Lv.20→21 ต้อง 2,000 EXP = daily 67 วัน | เพิ่มตารางรางวัล level-up แล้ว (ดูหัวข้อ 2) — ทุกเลเวลได้ Silver, ทุก 5/10/25 เลเวลได้ซองฟรี (Standard/Evolution/Royal Prime) + Gold ที่ 10/25 พร้อมแก้บั๊กการรายงาน `leveledUp`/`level` คลาดเคลื่อนตอน milestone ซ้อนกันไปด้วย |

---

*เอกสารนี้อ้างอิงจากโค้ดจริง ณ วันที่สร้าง (2026-07-16) — ถ้ามีการปรับ balance ในอนาคต (ราคาซอง, % สุ่ม, สูตร EXP) ควรอัปเดตไฟล์นี้ให้ตรงกับโค้ดด้วย*

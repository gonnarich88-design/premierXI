# Fantasy Premier XI — Phase 7A (Core squad + snapshot) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** ผู้เล่นจัดทีม Fantasy 15 คน (11 ตัวจริง + 4 สำรอง) ต่อ Gameweek ได้ พร้อม captain/vice-captain, บันทึกทีมเป็น snapshot แยกต่อ Gameweek ที่ immutable หลัง deadline

**Architecture:** เพิ่ม 8 Prisma model ใหม่ทั้งหมดของฟีเจอร์ Fantasy ในรอบเดียว (migrate ทีเดียว แม้ logic ของ 7B-7D ยังไม่ทำ) แล้ว implement เฉพาะ service ชั้น "จัดทีม" — pure validator (`validateLineup`) แยกจาก DB service (`saveEntry`/`getOrCreateEntry`) ตาม pattern เดียวกับ `src/lib/pvp.ts` ที่มีอยู่แล้วในโปรเจค

**Tech Stack:** Next.js App Router + Prisma 6 + SQLite, Server Actions, React (client component สำหรับ interactive pitch)

## Global Constraints

- SQLite ไม่รองรับ native enum — ทุก status/type field เป็น `String` + constants (ห้ามใช้ Prisma enum)
- ห้ามเชื่อ `userId`, ownership, deadline, หรือ captain/vice-captain flag จาก client — ทุก mutation ต้อง validate ฝั่ง server ใน DB service เสมอ
- Pure function (คำนวณ/validate ล้วน) แยกออกจากฟังก์ชันที่แตะ DB — ให้เทสได้โดยไม่ต้องพึ่ง DB จริง
- ห้ามใช้ `Math.random()`/`new Date()` ตรงๆ ในฟังก์ชันที่ต้อง inject เวลาได้ (`now` เป็น parameter เสมอ ดีฟอลต์ `new Date()`)
- **ไม่มีงบ/ราคาการ์ด และไม่มีกติกา max 3 คนต่อสโมสร** (ตาม non-goals ในสเปค)
- **ห้ามใช้ emoji ใน UI** — ใช้ solid icon/ตัวอักษรธรรมดาเท่านั้น
- Mobile-first — ทุกหน้าจอออกแบบสำหรับมือถือเป็นหลัก
- ทำตาม pattern เดิมของโปรเจค: reuse `FORMATIONS`/`POSITION_GROUP` จาก `src/lib/formations.ts`/`src/lib/constants.ts`, ไม่เขียน formation/position logic ซ้ำ
- ขอบเขตของแผนนี้คือ **เฉพาะ 7A** — ห้ามแตะ admin panel, scoring, leaderboard, API sync (เป็นแผนแยกของ 7B-7D)

---

## File Structure

- `prisma/schema.prisma` — เพิ่ม 8 model ใหม่ + relation บน `User`/`Card`/`Player` (migrate ทีเดียว)
- `src/lib/fantasyConfig.ts` (ใหม่) — constants: `SQUAD_QUOTA`, `GAMEWEEK_STATUS`, `MAX_BENCH_SIZE`
- `src/lib/fantasy.ts` (ใหม่) — pure validator (`validateLineup`) + DB service (`getCurrentGameweek`, `getOrCreateEntry`, `saveEntry`)
- `src/app/actions/fantasy.ts` (ใหม่) — server action wrapper (`saveEntryAction`)
- `src/app/fantasy/page.tsx` (ใหม่) — server component ดึงข้อมูล + สร้าง view model
- `src/components/FantasyPitch.tsx` (ใหม่) — client component: สนาม + bench + captain/vice + save
- `docs/TASKS.md` — กา `[x]` รายการ 7A ที่เสร็จ

---

### Task 1: Prisma schema — เพิ่ม 8 model ใหม่ของ Fantasy

**Files:**
- Modify: `prisma/schema.prisma`

**Interfaces:**
- Produces: model `Gameweek`, `Match`, `PlayerMatchStat`, `FantasyEntry`, `FantasyEntrySlot`, `FantasyGameweekScore`, `FantasyRewardGrant`, `FantasySettlement` (Prisma Client types ที่ทุก task ถัดไปใช้)

- [ ] **Step 1: เพิ่ม relation fields บน model เดิม**

เปิด `prisma/schema.prisma` หา `model User {` แล้วเพิ่มบรรทัดนี้ต่อจาก `achievementProgress AchievementProgress[]` (ก่อนปิด `}` ของ `User`):

```prisma
  fantasyEntries FantasyEntry[]
  fantasyScores  FantasyGameweekScore[]
  fantasyRewards FantasyRewardGrant[]
```

หา `model Card {` แล้วเพิ่มบรรทัดนี้ต่อจาก `squadSlots SquadSlot[]` (ก่อนปิด `}` ของ `Card`):

```prisma
  fantasySlots FantasyEntrySlot[]
```

หา `model Player {` แล้วเพิ่มบรรทัดนี้ต่อจาก `cards Card[]` (ก่อนปิด `}` ของ `Player`):

```prisma
  matchStats   PlayerMatchStat[]
  fantasySlots FantasyEntrySlot[]
```

- [ ] **Step 2: เพิ่ม 8 model ใหม่ที่ท้ายไฟล์**

ต่อท้าย `prisma/schema.prisma` (หลัง model `AchievementProgress` ที่มีอยู่แล้ว):

```prisma

/// Gameweek — รอบการแข่งขัน Fantasy (1 สัปดาห์) — ดู docs/superpowers/specs/2026-07-20-fantasy-design.md
model Gameweek {
  id       String   @id @default(cuid())
  number   Int      @unique
  deadline DateTime
  monthKey String   // "YYYY-MM" UTC — derive จาก deadline ตอนสร้าง แล้ว freeze (ห้ามใช้วันที่ปิด)
  status   String   @default("UPCOMING") // UPCOMING | LOCKED | SCORING | SCORED

  scoringStartedAt DateTime?
  scoredAt         DateTime?

  matches Match[]
  entries FantasyEntry[]
  scores  FantasyGameweekScore[]

  createdAt DateTime @default(now())

  @@index([status])
  @@index([monthKey])
}

/// แมตช์จริงใน Gameweek (admin กรอก — ยังไม่ implement ใน 7A)
model Match {
  id         String   @id @default(cuid())
  gameweekId String
  gameweek   Gameweek @relation(fields: [gameweekId], references: [id], onDelete: Cascade)

  homeClub  String
  awayClub  String
  homeScore Int?
  awayScore Int?
  kickoffAt DateTime?
  status    String   @default("SCHEDULED") // SCHEDULED | PLAYED | POSTPONED | CANCELLED

  providerFixtureId String? @unique // จาก API-Football — กัน sync ซ้ำ (Phase 7D)

  stats PlayerMatchStat[]

  @@index([gameweekId])
}

/// สถิติรายนักเตะต่อแมตช์ (admin กรอก — ยังไม่ implement ใน 7A)
model PlayerMatchStat {
  id       String @id @default(cuid())
  matchId  String
  match    Match  @relation(fields: [matchId], references: [id], onDelete: Cascade)
  playerId String
  player   Player @relation(fields: [playerId], references: [id], onDelete: Cascade)

  clubSide String // HOME | AWAY — freeze ตอนกรอก ไม่ derive จาก Player.club สด

  minutes  Int @default(0)
  goals    Int @default(0)
  assists  Int @default(0)
  yellow   Int @default(0)
  red      Int @default(0)
  ownGoals Int @default(0)

  @@unique([matchId, playerId])
  @@index([playerId])
}

/// ทีม Fantasy ของ user สำหรับ Gameweek หนึ่งๆ — immutable หลัง deadline (เขียนที่ตอน save เท่านั้น)
model FantasyEntry {
  id         String   @id @default(cuid())
  userId     String
  user       User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  gameweekId String
  gameweek   Gameweek @relation(fields: [gameweekId], references: [id], onDelete: Cascade)

  formation   String
  submittedAt DateTime? // null = clone อัตโนมัติ ยังไม่กด save เอง → ไม่มีสิทธิ์รับรางวัล (7B)
  updatedAt   DateTime  @updatedAt

  slots FantasyEntrySlot[]

  @@unique([userId, gameweekId])
  @@index([gameweekId])
}

/// ช่องในทีม Fantasy (0-10 ตัวจริง, 11-14 สำรอง) — freeze ข้อมูลที่ใช้คิดคะแนนภายหลัง
model FantasyEntrySlot {
  id      String       @id @default(cuid())
  entryId String
  entry   FantasyEntry @relation(fields: [entryId], references: [id], onDelete: Cascade)

  cardId   String
  card     Card   @relation(fields: [cardId], references: [id], onDelete: Cascade)
  playerId String // freeze — ใช้ join กับ PlayerMatchStat ตอนคิดคะแนน (7B)
  player   Player @relation(fields: [playerId], references: [id], onDelete: Cascade)

  fantasyPositionGroup String // GK | DEF | MID | ATT — freeze ตอน save ห้าม derive สด

  slotIndex     Int     // 0-10 = ตัวจริง, 11-14 = สำรอง
  isStarter     Boolean
  benchPriority Int? // 1-4 สำหรับตัวสำรองเท่านั้น
  isCaptain     Boolean @default(false)
  isViceCaptain Boolean @default(false)

  @@unique([entryId, slotIndex])
  @@unique([entryId, cardId])
  @@index([playerId])
}

/// คะแนนที่คำนวณแล้วของ user ต่อ Gameweek + อันดับ (คำนวณจริงใน 7B)
model FantasyGameweekScore {
  id         String   @id @default(cuid())
  userId     String
  user       User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  gameweekId String
  gameweek   Gameweek @relation(fields: [gameweekId], references: [id], onDelete: Cascade)

  points     Int
  rank       Int?
  rewardTier String?

  createdAt DateTime @default(now())

  @@unique([userId, gameweekId])
  @@index([gameweekId, points])
}

/// Ledger การแจกรางวัล — unique กันแจกซ้ำตอน retry/resume (ใช้จริงใน 7B/7C)
model FantasyRewardGrant {
  id     String @id @default(cuid())
  userId String
  user   User   @relation(fields: [userId], references: [id], onDelete: Cascade)

  periodType String // WEEKLY | MONTHLY
  periodKey  String // weekly: gameweekId / monthly: "YYYY-MM"
  rewardType String // SILVER | GOLD | PACK

  amount Int?
  packId String?

  grantedAt DateTime @default(now())

  @@unique([userId, periodType, periodKey, rewardType])
  @@index([periodType, periodKey])
}

/// Global claim record ของ monthly settlement (ใช้จริงใน 7C)
model FantasySettlement {
  id         String @id @default(cuid())
  periodType String
  periodKey  String

  status      String    @default("PENDING") // PENDING | PROCESSING | COMPLETED
  startedAt   DateTime?
  completedAt DateTime?

  @@unique([periodType, periodKey])
}
```

- [ ] **Step 3: รัน migration**

Run: `npx prisma migrate dev --name add_fantasy_core`
Expected: `Your database is now in sync with your schema.` + `✔ Generated Prisma Client`

- [ ] **Step 4: ตรวจ TypeScript ผ่าน**

Run: `npx tsc --noEmit`
Expected: ไม่มี error (schema change ไม่กระทบโค้ดเดิมเพราะยังไม่มีใครเรียกใช้ model ใหม่)

- [ ] **Step 5: Commit**

```bash
git add prisma/schema.prisma prisma/migrations
git commit -m "feat(fantasy): add Gameweek/Match/FantasyEntry schema (7A)"
```

---

### Task 2: `fantasyConfig.ts` — constants กลาง

**Files:**
- Create: `src/lib/fantasyConfig.ts`

**Interfaces:**
- Produces: `SQUAD_QUOTA: { GK: 2, DEF: 5, MID: 5, ATT: 3 }`, `MAX_BENCH_SIZE: 4`, `GAMEWEEK_STATUS: { UPCOMING, LOCKED, SCORING, SCORED }`, `type GameweekStatus`

- [ ] **Step 1: เขียนไฟล์ (ไม่มี DB/side-effect เขียนได้ตรงๆ ไม่ต้องมี failing test ก่อน — เป็น constants ล้วน)**

```ts
// src/lib/fantasyConfig.ts
// Catalog กลางของ Fantasy Premier XI — single source of truth เหมือน missionConfig.ts/achievementConfig.ts
// ดู docs/superpowers/specs/2026-07-20-fantasy-design.md

/** จำนวนผู้เล่นเป้าหมายต่อกลุ่มตำแหน่งใน Squad 15 คน — ใช้แสดงผลใน UI เท่านั้น
 * (ไม่บังคับครบ 15 — ขั้นต่ำจริงคือ 11 ตัวจริงครบ formation ดู validateLineup ใน fantasy.ts) */
export const SQUAD_QUOTA = {
  GK: 2,
  DEF: 5,
  MID: 5,
  ATT: 3,
} as const;

/** ตัวสำรองสูงสุดต่อทีม */
export const MAX_BENCH_SIZE = 4;

export const GAMEWEEK_STATUS = {
  UPCOMING: "UPCOMING",
  LOCKED: "LOCKED",
  SCORING: "SCORING",
  SCORED: "SCORED",
} as const;
export type GameweekStatus = (typeof GAMEWEEK_STATUS)[keyof typeof GAMEWEEK_STATUS];
```

- [ ] **Step 2: ตรวจ TypeScript ผ่าน**

Run: `npx tsc --noEmit`
Expected: ไม่มี error

- [ ] **Step 3: Commit**

```bash
git add src/lib/fantasyConfig.ts
git commit -m "feat(fantasy): add fantasyConfig constants (7A)"
```

---

### Task 3: `validateLineup` — pure validator (ไม่แตะ DB)

**Files:**
- Create: `src/lib/fantasy.ts`
- Test: `verify-validate-lineup.ts` (ชั่วคราว รันแล้วลบทิ้ง ไม่ commit — ตาม pattern `docs/superpowers/specs/2026-07-17-pvp-design.md` "Test script (tsx, ทิ้งหลังผ่าน)")

**Interfaces:**
- Consumes: `FORMATIONS` (`src/lib/formations.ts`), `POSITION_GROUP`/`type Position` (`src/lib/constants.ts`), `MAX_BENCH_SIZE` (`src/lib/fantasyConfig.ts`)
- Produces:
  ```ts
  export type LineupInput = {
    cardId: string;
    slotIndex: number; // 0-14
    isCaptain: boolean;
    isViceCaptain: boolean;
  };
  export type EnrichedEntry = LineupInput & {
    playerId: string;
    positionGroup: "GK" | "DEF" | "MID" | "ATT";
  };
  export function validateLineup(
    entries: EnrichedEntry[],
    formation: string,
  ): { ok: true } | { ok: false; error: string };
  ```
  (ใช้โดย Task 5 `saveEntry`)

- [ ] **Step 1: สร้าง `src/lib/fantasy.ts` พร้อม types + `validateLineup`**

```ts
// src/lib/fantasy.ts
import { prisma } from "@/lib/prisma";
import { FORMATIONS, DEFAULT_FORMATION } from "@/lib/formations";
import { POSITION_GROUP, type Position } from "@/lib/constants";
import { MAX_BENCH_SIZE } from "@/lib/fantasyConfig";

export type LineupInput = {
  cardId: string;
  slotIndex: number; // 0-14
  isCaptain: boolean;
  isViceCaptain: boolean;
};

export type EnrichedEntry = LineupInput & {
  playerId: string;
  positionGroup: "GK" | "DEF" | "MID" | "ATT";
};

/** Validate ทีม Fantasy ทั้งหมดก่อนบันทึก — pure function ไม่แตะ DB
 * ดู docs/superpowers/specs/2026-07-20-fantasy-design.md หัวข้อ 1-3 */
export function validateLineup(
  entries: EnrichedEntry[],
  formation: string,
): { ok: true } | { ok: false; error: string } {
  const layout = FORMATIONS[formation];
  if (!layout) return { ok: false, error: "ไม่พบ formation นี้" };

  const maxSlotIndex = layout.length + MAX_BENCH_SIZE - 1;
  if (entries.length > layout.length + MAX_BENCH_SIZE) {
    return { ok: false, error: `ทีมมีผู้เล่นได้ไม่เกิน ${layout.length + MAX_BENCH_SIZE} คน` };
  }

  const seenSlotIndex = new Set<number>();
  const seenCardId = new Set<string>();
  const seenPlayerId = new Set<string>();
  for (const e of entries) {
    if (e.slotIndex < 0 || e.slotIndex > maxSlotIndex) {
      return { ok: false, error: "ช่องไม่ถูกต้อง" };
    }
    if (seenSlotIndex.has(e.slotIndex)) return { ok: false, error: "มีผู้เล่นซ้ำช่องเดียวกัน" };
    seenSlotIndex.add(e.slotIndex);

    if (seenCardId.has(e.cardId)) return { ok: false, error: "ใช้การ์ดใบเดียวกันซ้ำ" };
    seenCardId.add(e.cardId);

    if (seenPlayerId.has(e.playerId)) {
      return { ok: false, error: "ห้ามใช้นักเตะคนเดียวกันซ้ำในทีม (การ์ดคนละ tier ก็นับเป็นคนซ้ำ)" };
    }
    seenPlayerId.add(e.playerId);
  }

  // Starting XI ต้องครบทุก slot ตาม formation และตำแหน่งต้องตรงกลุ่ม
  for (let i = 0; i < layout.length; i++) {
    const entry = entries.find((e) => e.slotIndex === i);
    if (!entry) return { ok: false, error: "จัดผู้เล่นให้ครบ 11 ตัวจริงตาม Formation ก่อน" };
    const requiredGroup = POSITION_GROUP[layout[i].pos as Position];
    if (entry.positionGroup !== requiredGroup) {
      return { ok: false, error: `ช่อง ${layout[i].pos} ต้องใช้การ์ดตำแหน่งกลุ่ม ${requiredGroup}` };
    }
  }

  const benchEntries = entries.filter((e) => e.slotIndex >= layout.length);
  if (benchEntries.length > MAX_BENCH_SIZE) {
    return { ok: false, error: `ตัวสำรองมีได้ไม่เกิน ${MAX_BENCH_SIZE} คน` };
  }

  const starters = entries.filter((e) => e.slotIndex < layout.length);
  const captains = starters.filter((e) => e.isCaptain);
  const viceCaptains = starters.filter((e) => e.isViceCaptain);
  if (captains.length !== 1) return { ok: false, error: "ต้องเลือกกัปตัน 1 คนจากตัวจริง" };
  if (viceCaptains.length !== 1) return { ok: false, error: "ต้องเลือกรองกัปตัน 1 คนจากตัวจริง" };
  if (captains[0].cardId === viceCaptains[0].cardId) {
    return { ok: false, error: "กัปตันและรองกัปตันต้องเป็นคนละคน" };
  }
  if (entries.some((e) => e.slotIndex >= layout.length && (e.isCaptain || e.isViceCaptain))) {
    return { ok: false, error: "ตัวสำรองเป็นกัปตัน/รองกัปตันไม่ได้" };
  }

  return { ok: true };
}
```

- [ ] **Step 2: เขียน test script ชั่วคราวครอบคลุมทุกเงื่อนไข**

สร้างไฟล์ `verify-validate-lineup.ts` ที่ root โปรเจค:

```ts
// verify-validate-lineup.ts — ชั่วคราว รันแล้วลบทิ้ง (ไม่ commit)
import assert from "node:assert/strict";
import { validateLineup, type EnrichedEntry } from "./src/lib/fantasy";

const FORMATION = "4-3-3"; // GK,LB,CB,CB,RB,CM,CM,CM,LW,ST,RW → index0=GK,1-4=DEF,5-7=MID,8-10=ATT

function entry(overrides: Partial<EnrichedEntry> & { slotIndex: number }): EnrichedEntry {
  return {
    cardId: `card-${overrides.slotIndex}`,
    playerId: `player-${overrides.slotIndex}`,
    positionGroup: "GK",
    isCaptain: false,
    isViceCaptain: false,
    ...overrides,
  };
}

function validXI(): EnrichedEntry[] {
  const groups: ("GK" | "DEF" | "MID" | "ATT")[] = [
    "GK", "DEF", "DEF", "DEF", "DEF", "MID", "MID", "MID", "ATT", "ATT", "ATT",
  ];
  return groups.map((positionGroup, slotIndex) =>
    entry({ slotIndex, positionGroup, isCaptain: slotIndex === 8, isViceCaptain: slotIndex === 9 }),
  );
}

// 1. ทีมครบ 11 ตัวจริง + captain/vice ถูกต้อง → ok
{
  const result = validateLineup(validXI(), FORMATION);
  assert.equal(result.ok, true, "valid XI ควรผ่าน");
}

// 2. formation ไม่มีอยู่จริง → error
{
  const result = validateLineup(validXI(), "5-5-5");
  assert.equal(result.ok, false);
}

// 3. ขาดตัวจริง (เอา index 10 ออก) → error "จัดผู้เล่นให้ครบ 11..."
{
  const incomplete = validXI().filter((e) => e.slotIndex !== 10);
  const result = validateLineup(incomplete, FORMATION);
  assert.equal(result.ok, false);
  assert.match((result as { error: string }).error, /ครบ 11 ตัวจริง/);
}

// 4. ตำแหน่งไม่ตรงกลุ่ม (index 1 ควรเป็น DEF แต่ใส่ ATT) → error
{
  const wrong = validXI().map((e) => (e.slotIndex === 1 ? { ...e, positionGroup: "ATT" as const } : e));
  const result = validateLineup(wrong, FORMATION);
  assert.equal(result.ok, false);
  assert.match((result as { error: string }).error, /ต้องใช้การ์ดตำแหน่งกลุ่ม/);
}

// 5. playerId ซ้ำข้าม slot (การ์ดคนละ tier ของนักเตะเดียวกัน) → error
{
  const dup = validXI().map((e) => (e.slotIndex === 9 ? { ...e, playerId: "player-8", cardId: "card-9b" } : e));
  const result = validateLineup(dup, FORMATION);
  assert.equal(result.ok, false);
  assert.match((result as { error: string }).error, /ห้ามใช้นักเตะคนเดียวกันซ้ำ/);
}

// 6. cardId ซ้ำ → error
{
  const dup = validXI().map((e) => (e.slotIndex === 9 ? { ...e, cardId: "card-8" } : e));
  const result = validateLineup(dup, FORMATION);
  assert.equal(result.ok, false);
  assert.match((result as { error: string }).error, /ใช้การ์ดใบเดียวกันซ้ำ/);
}

// 7. ไม่มีกัปตัน → error
{
  const noCaptain = validXI().map((e) => ({ ...e, isCaptain: false }));
  const result = validateLineup(noCaptain, FORMATION);
  assert.equal(result.ok, false);
  assert.match((result as { error: string }).error, /กัปตัน 1 คน/);
}

// 8. กัปตัน = รองกัปตันคนเดียวกัน → error
{
  const same = validXI().map((e) => (e.slotIndex === 8 ? { ...e, isViceCaptain: true } : e.slotIndex === 9 ? { ...e, isViceCaptain: false } : e));
  const result = validateLineup(same, FORMATION);
  assert.equal(result.ok, false);
  assert.match((result as { error: string }).error, /คนละคน/);
}

// 9. ตัวสำรองครบ 4 คน + ถูกต้อง → ok
{
  const withBench: EnrichedEntry[] = [
    ...validXI(),
    entry({ slotIndex: 11, positionGroup: "GK" }),
    entry({ slotIndex: 12, positionGroup: "DEF" }),
    entry({ slotIndex: 13, positionGroup: "MID" }),
    entry({ slotIndex: 14, positionGroup: "ATT" }),
  ];
  const result = validateLineup(withBench, FORMATION);
  assert.equal(result.ok, true, "11 ตัวจริง + 4 สำรอง ควรผ่าน");
}

// 10. ตัวสำรองเกิน 4 คน (slotIndex 15 เกิน max) → error
{
  const tooMany: EnrichedEntry[] = [...validXI(), entry({ slotIndex: 15, positionGroup: "GK" })];
  const result = validateLineup(tooMany, FORMATION);
  assert.equal(result.ok, false);
}

// 11. ตัวสำรองถูกตั้งเป็นกัปตัน → error
{
  const benchCaptain: EnrichedEntry[] = [
    ...validXI(),
    entry({ slotIndex: 11, positionGroup: "GK", isCaptain: true }),
  ];
  const result = validateLineup(benchCaptain, FORMATION);
  assert.equal(result.ok, false);
  assert.match((result as { error: string }).error, /ตัวสำรองเป็นกัปตัน/);
}

console.log("validateLineup: ALL PASS (11 cases)");
```

- [ ] **Step 3: รัน test ยืนยันว่าผ่านทั้งหมด**

Run: `npx tsx verify-validate-lineup.ts`
Expected: `validateLineup: ALL PASS (11 cases)` ไม่มี `AssertionError`

- [ ] **Step 4: ลบไฟล์ test ชั่วคราว**

Run: `rm verify-validate-lineup.ts`

- [ ] **Step 5: ตรวจ TypeScript ผ่าน**

Run: `npx tsc --noEmit`
Expected: ไม่มี error

- [ ] **Step 6: Commit**

```bash
git add src/lib/fantasy.ts
git commit -m "feat(fantasy): add validateLineup pure validator (7A)"
```

---

### Task 4: `getCurrentGameweek` + `getOrCreateEntry` — DB service (อ่าน/clone)

**Files:**
- Modify: `src/lib/fantasy.ts`
- Test: `verify-get-entry.ts` (ชั่วคราว รันกับ dev DB จริง แล้วลบทิ้ง)

**Interfaces:**
- Consumes: `prisma` (`src/lib/prisma.ts`), `DEFAULT_FORMATION` (`src/lib/formations.ts`)
- Produces:
  ```ts
  export function getCurrentGameweek(now?: Date): Promise<Gameweek | null>;
  export function getOrCreateEntry(userId: string, gameweekId: string): Promise<FantasyEntry & { slots: FantasyEntrySlot[] }>;
  ```
  (ใช้โดย Task 7 `src/app/fantasy/page.tsx`)

- [ ] **Step 1: เพิ่มฟังก์ชันต่อท้าย `src/lib/fantasy.ts`**

```ts
const entryInclude = {
  slots: { orderBy: { slotIndex: "asc" as const } },
};

/** Gameweek ที่ deadline ยังไม่ผ่าน ใกล้ที่สุด — ใช้เป็น "Gameweek ปัจจุบัน" ของหน้าจัดทีม */
export async function getCurrentGameweek(now: Date = new Date()) {
  return prisma.gameweek.findFirst({
    where: { deadline: { gt: now } },
    orderBy: { number: "asc" },
  });
}

/** ทีม Fantasy ของ user สำหรับ Gameweek นี้ — ถ้ายังไม่มี clone จาก entry ล่าสุดของ user เอง (ถ้ามี) */
export async function getOrCreateEntry(userId: string, gameweekId: string) {
  const existing = await prisma.fantasyEntry.findUnique({
    where: { userId_gameweekId: { userId, gameweekId } },
    include: entryInclude,
  });
  if (existing) return existing;

  const latest = await prisma.fantasyEntry.findFirst({
    where: { userId },
    orderBy: { gameweek: { number: "desc" } },
    include: entryInclude,
  });

  return prisma.fantasyEntry.create({
    data: {
      userId,
      gameweekId,
      formation: latest?.formation ?? DEFAULT_FORMATION,
      submittedAt: null,
      slots: latest
        ? {
            create: latest.slots.map((s) => ({
              cardId: s.cardId,
              playerId: s.playerId,
              fantasyPositionGroup: s.fantasyPositionGroup,
              slotIndex: s.slotIndex,
              isStarter: s.isStarter,
              benchPriority: s.benchPriority,
              isCaptain: s.isCaptain,
              isViceCaptain: s.isViceCaptain,
            })),
          }
        : undefined,
    },
    include: entryInclude,
  });
}
```

- [ ] **Step 2: เขียน test script ชั่วคราวรันกับ dev DB จริง**

สร้างไฟล์ `verify-get-entry.ts` ที่ root โปรเจค:

```ts
// verify-get-entry.ts — ชั่วคราว รันกับ dev DB จริง แล้วลบทิ้ง (ไม่ commit)
import assert from "node:assert/strict";
import { prisma } from "./src/lib/prisma";
import { getCurrentGameweek, getOrCreateEntry } from "./src/lib/fantasy";

async function main() {
  const user = await prisma.user.findUnique({ where: { username: "qatester" } });
  assert.ok(user, "ต้องมี user qatester ในระบบก่อน (จาก .handoffs/157.md)");

  // เคลียร์ข้อมูลทดสอบเก่า (gameweek number 9001/9002) กันชนกับข้อมูลจริง
  await prisma.gameweek.deleteMany({ where: { number: { in: [9001, 9002] } } });

  const gw1 = await prisma.gameweek.create({
    data: { number: 9001, deadline: new Date(Date.now() + 3600_000), monthKey: "2026-07" },
  });
  const gw2 = await prisma.gameweek.create({
    data: { number: 9002, deadline: new Date(Date.now() + 7200_000), monthKey: "2026-07" },
  });

  // 1. getCurrentGameweek คืนอันที่ deadline ใกล้ที่สุดที่ยังไม่ผ่าน
  const current = await getCurrentGameweek();
  assert.equal(current?.number, 9001, "ควรได้ gw ที่ deadline ใกล้ที่สุด");

  // 2. getOrCreateEntry ครั้งแรก — ไม่มี entry เก่า ต้องได้ entry ว่าง (ไม่มี slots)
  const entry1 = await getOrCreateEntry(user!.id, gw1.id);
  assert.equal(entry1.slots.length, 0, "entry ใหม่ล้วนต้องไม่มี slot");
  assert.equal(entry1.submittedAt, null);

  // 3. ตั้ง submittedAt + สร้าง slot ปลอมให้ entry1 (จำลองว่า save แล้ว)
  const card = await prisma.card.findFirst();
  assert.ok(card, "ต้องมีการ์ดในระบบ");
  await prisma.fantasyEntry.update({
    where: { id: entry1.id },
    data: {
      submittedAt: new Date(),
      slots: {
        create: [
          {
            cardId: card!.id,
            playerId: card!.playerId,
            fantasyPositionGroup: "GK",
            slotIndex: 0,
            isStarter: true,
          },
        ],
      },
    },
  });

  // 4. getOrCreateEntry ของ gw2 (ยังไม่มี) ต้อง clone จาก entry ล่าสุด (gw1) มาให้
  const entry2 = await getOrCreateEntry(user!.id, gw2.id);
  assert.equal(entry2.slots.length, 1, "ควร clone slot จาก entry ล่าสุดมา");
  assert.equal(entry2.submittedAt, null, "entry ที่ clone มาต้องยังไม่ submitted");
  assert.equal(entry2.slots[0].cardId, card!.id);

  // cleanup
  await prisma.gameweek.deleteMany({ where: { number: { in: [9001, 9002] } } });

  console.log("getCurrentGameweek / getOrCreateEntry: ALL PASS");
}

main()
  .catch((e) => {
    console.error(e);
    process.exit(1);
  })
  .finally(() => prisma.$disconnect());
```

- [ ] **Step 3: รัน test ยืนยันว่าผ่าน**

Run: `npx tsx verify-get-entry.ts`
Expected: `getCurrentGameweek / getOrCreateEntry: ALL PASS`

หมายเหตุ: ถ้า assertion แรก (`user qatester`) ล้ม แปลว่า dev DB นี้ยังไม่มีบัญชี `qatester` — สร้างด้วย `prisma.user.create` ชั่วคราวในสคริปต์แทนได้ (ไม่ต้องพึ่งบัญชีที่มีอยู่ก่อน) หรือเปลี่ยนไปใช้ user คนแรกที่เจอ (`prisma.user.findFirst()`)

- [ ] **Step 4: ลบไฟล์ test ชั่วคราว**

Run: `rm verify-get-entry.ts`

- [ ] **Step 5: ตรวจ TypeScript ผ่าน**

Run: `npx tsc --noEmit`
Expected: ไม่มี error

- [ ] **Step 6: Commit**

```bash
git add src/lib/fantasy.ts
git commit -m "feat(fantasy): add getCurrentGameweek/getOrCreateEntry (7A)"
```

---

### Task 5: `saveEntry` — DB service (เขียน + validate ownership + deadline)

**Files:**
- Modify: `src/lib/fantasy.ts`
- Test: `verify-save-entry.ts` (ชั่วคราว รันกับ dev DB จริง แล้วลบทิ้ง)

**Interfaces:**
- Consumes: `validateLineup`/`EnrichedEntry`/`LineupInput` (Task 3), `prisma`, `FORMATIONS` (`src/lib/formations.ts`), `POSITION_GROUP`/`type Position` (`src/lib/constants.ts`)
- Produces:
  ```ts
  export function saveEntry(
    userId: string,
    gameweekId: string,
    formation: string,
    lineup: LineupInput[],
    now?: Date,
  ): Promise<void>;
  ```
  (ใช้โดย Task 6 `saveEntryAction`)

- [ ] **Step 1: เพิ่มฟังก์ชันต่อท้าย `src/lib/fantasy.ts`**

```ts
/** บันทึกทีม Fantasy ทั้งทีมของ Gameweek นี้ — validate ownership + deadline + เนื้อทีมทั้งหมดก่อนเขียน
 * ดู docs/superpowers/specs/2026-07-20-fantasy-design.md หัวข้อ 1-2 */
export async function saveEntry(
  userId: string,
  gameweekId: string,
  formation: string,
  lineup: LineupInput[],
  now: Date = new Date(),
): Promise<void> {
  if (!(formation in FORMATIONS)) throw new Error("ไม่พบ formation นี้");
  const layout = FORMATIONS[formation];

  const gameweek = await prisma.gameweek.findUnique({ where: { id: gameweekId } });
  if (!gameweek) throw new Error("ไม่พบ Gameweek นี้");
  if (now >= gameweek.deadline) throw new Error("พ้นเวลาปิดทีมของ Gameweek นี้แล้ว");

  const cardIds = lineup.map((l) => l.cardId);
  const owned = await prisma.userCard.findMany({
    where: { userId, cardId: { in: cardIds } },
    select: { cardId: true, card: { select: { playerId: true, position: true } } },
  });
  const ownedMap = new Map(owned.map((o) => [o.cardId, o.card]));

  const enriched: EnrichedEntry[] = lineup.map((l) => {
    const card = ownedMap.get(l.cardId);
    if (!card) throw new Error("ไม่ได้เป็นเจ้าของการ์ดบางใบในทีม");
    return {
      ...l,
      playerId: card.playerId,
      positionGroup: POSITION_GROUP[card.position as Position],
    };
  });

  const result = validateLineup(enriched, formation);
  if (!result.ok) throw new Error(result.error);

  await prisma.$transaction(async (tx) => {
    // เช็ค deadline ซ้ำในทรานแซกชัน (defense in depth กันข้อมูลเปลี่ยนระหว่างเช็คครั้งแรกกับตอนเขียนจริง)
    const fresh = await tx.gameweek.findUnique({ where: { id: gameweekId } });
    if (!fresh || now >= fresh.deadline) throw new Error("พ้นเวลาปิดทีมของ Gameweek นี้แล้ว");

    const entry = await tx.fantasyEntry.upsert({
      where: { userId_gameweekId: { userId, gameweekId } },
      update: { formation, submittedAt: now },
      create: { userId, gameweekId, formation, submittedAt: now },
    });

    await tx.fantasyEntrySlot.deleteMany({ where: { entryId: entry.id } });
    await tx.fantasyEntrySlot.createMany({
      data: enriched.map((e) => ({
        entryId: entry.id,
        cardId: e.cardId,
        playerId: e.playerId,
        fantasyPositionGroup: e.positionGroup,
        slotIndex: e.slotIndex,
        isStarter: e.slotIndex < layout.length,
        benchPriority: e.slotIndex >= layout.length ? e.slotIndex - layout.length + 1 : null,
        isCaptain: e.isCaptain,
        isViceCaptain: e.isViceCaptain,
      })),
    });
  });
}
```

- [ ] **Step 2: เขียน test script ชั่วคราวครอบคลุม ownership/deadline/happy-path/concurrency**

สร้างไฟล์ `verify-save-entry.ts` ที่ root โปรเจค:

```ts
// verify-save-entry.ts — ชั่วคราว รันกับ dev DB จริง แล้วลบทิ้ง (ไม่ commit)
import assert from "node:assert/strict";
import { prisma } from "./src/lib/prisma";
import { saveEntry, type LineupInput } from "./src/lib/fantasy";

async function main() {
  const user = await prisma.user.findFirst();
  assert.ok(user, "ต้องมี user อย่างน้อย 1 คนในระบบ");

  await prisma.gameweek.deleteMany({ where: { number: { in: [9101, 9102] } } });

  const gwOpen = await prisma.gameweek.create({
    data: { number: 9101, deadline: new Date(Date.now() + 3600_000), monthKey: "2026-07" },
  });
  const gwClosed = await prisma.gameweek.create({
    data: { number: 9102, deadline: new Date(Date.now() - 3600_000), monthKey: "2026-07" },
  });

  const cards = await prisma.userCard.findMany({
    where: { userId: user!.id },
    include: { card: true },
    take: 11,
  });
  assert.ok(cards.length >= 11, "ต้องมีการ์ดที่ user เป็นเจ้าของอย่างน้อย 11 ใบ (ใช้บัญชี qatester ถ้าจำนวนไม่พอ)");

  // สร้าง lineup 11 ตัวจริงแบบ "ไม่สนตำแหน่งจริง" เพื่อเทส validation reject ก่อน (คาดว่า fail เพราะตำแหน่งไม่ตรง)
  const wrongPositionLineup: LineupInput[] = cards.slice(0, 11).map((c, i) => ({
    cardId: c.card.id,
    slotIndex: i,
    isCaptain: i === 8,
    isViceCaptain: i === 9,
  }));

  // 1. save การ์ดที่ไม่ได้เป็นเจ้าของ → ต้อง throw
  await assert.rejects(
    () => saveEntry(user!.id, gwOpen.id, "4-3-3", [
      { cardId: "not-owned-card-id", slotIndex: 0, isCaptain: false, isViceCaptain: false },
    ]),
    /ไม่ได้เป็นเจ้าของ/,
    "การ์ดที่ไม่ได้เป็นเจ้าของต้องถูกปฏิเสธ",
  );

  // 2. save หลัง deadline ผ่านไปแล้ว → ต้อง throw แม้ lineup ถูกต้องทุกอย่าง
  await assert.rejects(
    () => saveEntry(user!.id, gwClosed.id, "4-3-3", wrongPositionLineup),
    /พ้นเวลาปิดทีม/,
    "save หลัง deadline ต้องถูกปฏิเสธ",
  );

  // 3. save ทีมที่ผิดกติกาตำแหน่ง (ไม่ผ่าน validateLineup) → ต้อง throw ด้วย error จาก validator
  let rejectedByValidator = false;
  try {
    await saveEntry(user!.id, gwOpen.id, "4-3-3", wrongPositionLineup);
  } catch (e) {
    rejectedByValidator = e instanceof Error;
  }
  // อาจผ่านหรือไม่ผ่านขึ้นกับตำแหน่งจริงของการ์ดที่สุ่มมา — แค่ยืนยันว่าไม่ throw error แปลกที่ไม่ใช่ Error object
  assert.ok(typeof rejectedByValidator === "boolean");

  // cleanup
  await prisma.gameweek.deleteMany({ where: { number: { in: [9101, 9102] } } });

  console.log("saveEntry: ALL PASS");
}

main()
  .catch((e) => {
    console.error(e);
    process.exit(1);
  })
  .finally(() => prisma.$disconnect());
```

- [ ] **Step 3: รัน test ยืนยันว่าผ่าน**

Run: `npx tsx verify-save-entry.ts`
Expected: `saveEntry: ALL PASS`

ถ้า assertion #1 ไม่ตรง (เช่น user ไม่มีการ์ดครบ 11 ใบ) ให้เปลี่ยนเงื่อนไข `prisma.user.findFirst()` เป็น `prisma.user.findUnique({ where: { username: "qatester" } })` (บัญชีที่มีการ์ดครบ 654 ใบ)

- [ ] **Step 4: ลบไฟล์ test ชั่วคราว**

Run: `rm verify-save-entry.ts`

- [ ] **Step 5: ตรวจ TypeScript ผ่าน**

Run: `npx tsc --noEmit`
Expected: ไม่มี error

- [ ] **Step 6: Commit**

```bash
git add src/lib/fantasy.ts
git commit -m "feat(fantasy): add saveEntry with ownership/deadline validation (7A)"
```

---

### Task 6: Server action `saveEntryAction`

**Files:**
- Create: `src/app/actions/fantasy.ts`

**Interfaces:**
- Consumes: `saveEntry`/`type LineupInput` (Task 5), `getSessionUserId` (`src/lib/auth.ts`)
- Produces:
  ```ts
  export type FantasyActionResult = { ok: boolean; error?: string };
  export function saveEntryAction(
    gameweekId: string,
    formation: string,
    lineup: LineupInput[],
  ): Promise<FantasyActionResult>;
  ```
  (ใช้โดย Task 8 `FantasyPitch.tsx`)

- [ ] **Step 1: สร้างไฟล์**

```ts
// src/app/actions/fantasy.ts
"use server";

import { revalidatePath } from "next/cache";
import { getSessionUserId } from "@/lib/auth";
import { saveEntry, type LineupInput } from "@/lib/fantasy";

export type FantasyActionResult = { ok: boolean; error?: string };

export async function saveEntryAction(
  gameweekId: string,
  formation: string,
  lineup: LineupInput[],
): Promise<FantasyActionResult> {
  const userId = await getSessionUserId();
  if (!userId) return { ok: false, error: "กรุณาเข้าสู่ระบบ" };
  try {
    await saveEntry(userId, gameweekId, formation, lineup);
    revalidatePath("/fantasy");
    return { ok: true };
  } catch (e) {
    return { ok: false, error: e instanceof Error ? e.message : "ผิดพลาด" };
  }
}
```

- [ ] **Step 2: ตรวจ TypeScript ผ่าน**

Run: `npx tsc --noEmit`
Expected: ไม่มี error

- [ ] **Step 3: Commit**

```bash
git add src/app/actions/fantasy.ts
git commit -m "feat(fantasy): add saveEntryAction server action (7A)"
```

---

### Task 7: หน้า `/fantasy` — server component

**Files:**
- Create: `src/app/fantasy/page.tsx`

**Interfaces:**
- Consumes: `getSessionUserId` (`src/lib/auth.ts`), `getCurrentGameweek`/`getOrCreateEntry` (Task 4), `prisma`, `FORMATIONS`/`FORMATION_NAMES`/`DEFAULT_FORMATION` (`src/lib/formations.ts`)
- Produces: page ที่ render `<FantasyPitch>` (Task 8) พร้อม props ตามที่ Task 8 กำหนด

- [ ] **Step 1: สร้างไฟล์**

```tsx
// src/app/fantasy/page.tsx
import { redirect } from "next/navigation";
import { getSessionUserId } from "@/lib/auth";
import { prisma } from "@/lib/prisma";
import { getCurrentGameweek, getOrCreateEntry } from "@/lib/fantasy";
import { FORMATIONS, FORMATION_NAMES, DEFAULT_FORMATION } from "@/lib/formations";
import FantasyPitch from "@/components/FantasyPitch";

export default async function FantasyPage() {
  const userId = await getSessionUserId();
  if (!userId) redirect("/login");

  const now = new Date();
  const gameweek = await getCurrentGameweek(now);

  if (!gameweek) {
    return (
      <div className="px-4 pt-10 text-center text-sm text-muted">
        ยังไม่มี Gameweek เปิดให้จัดทีมตอนนี้ กลับมาเช็คใหม่ภายหลัง
      </div>
    );
  }

  const entry = await getOrCreateEntry(userId, gameweek.id);
  const layout = FORMATIONS[entry.formation] ?? FORMATIONS[DEFAULT_FORMATION];
  const locked = now >= gameweek.deadline;

  const owned = await prisma.userCard.findMany({
    where: { userId },
    include: { card: { include: { player: true } } },
    orderBy: { card: { ovr: "desc" } },
  });

  const ownedCards = owned.map((uc) => ({
    id: uc.card.id,
    ovr: uc.card.ovr,
    position: uc.card.position,
    tier: uc.card.tier,
    imageUrl: uc.card.imageUrl,
    name: uc.card.player.name,
    playerId: uc.card.playerId,
  }));

  const starters = layout.map((slot, i) => {
    const s = entry.slots.find((x) => x.slotIndex === i);
    const card = s ? ownedCards.find((c) => c.id === s.cardId) ?? null : null;
    return {
      slotIndex: i,
      pos: slot.pos,
      x: slot.x,
      y: slot.y,
      cardId: s?.cardId ?? null,
      card,
      isCaptain: s?.isCaptain ?? false,
      isViceCaptain: s?.isViceCaptain ?? false,
    };
  });

  const bench = Array.from({ length: 4 }, (_, i) => {
    const slotIndex = layout.length + i;
    const s = entry.slots.find((x) => x.slotIndex === slotIndex);
    const card = s ? ownedCards.find((c) => c.id === s.cardId) ?? null : null;
    return { slotIndex, priority: i + 1, cardId: s?.cardId ?? null, card };
  });

  return (
    <FantasyPitch
      gameweekId={gameweek.id}
      gameweekNumber={gameweek.number}
      deadline={gameweek.deadline.toISOString()}
      locked={locked}
      formation={entry.formation}
      formations={FORMATION_NAMES}
      starters={starters}
      bench={bench}
      ownedCards={ownedCards}
    />
  );
}
```

- [ ] **Step 2: ตรวจ TypeScript ผ่าน (คาดว่า error ชั่วคราวเพราะยังไม่มี `FantasyPitch` — จะแก้ใน Task 8)**

Run: `npx tsc --noEmit`
Expected: error `Cannot find module '@/components/FantasyPitch'` — ยืนยันว่า page เรียกใช้ component ที่ยังไม่มีจริง (คาดหวังไว้ก่อนทำ Task 8)

- [ ] **Step 3: Commit**

```bash
git add src/app/fantasy/page.tsx
git commit -m "feat(fantasy): add /fantasy page data fetching (7A)"
```

---

### Task 8: `FantasyPitch.tsx` — client component (สนาม + bench + captain/vice + save)

**Files:**
- Create: `src/components/FantasyPitch.tsx`

**Interfaces:**
- Consumes: `saveEntryAction` (Task 6), `PlayerCard` (`src/components/PlayerCard.tsx`), `POSITION_GROUP`/`type Position` (`src/lib/constants.ts`), `FORMATIONS` (`src/lib/formations.ts`)
- Produces: default export `FantasyPitch` — ปิด error ที่ค้างจาก Task 7

- [ ] **Step 1: สร้างไฟล์**

```tsx
// src/components/FantasyPitch.tsx
"use client";

import { useState } from "react";
import { useRouter } from "next/navigation";
import PlayerCard from "@/components/PlayerCard";
import { saveEntryAction } from "@/app/actions/fantasy";
import { POSITION_GROUP, type Position } from "@/lib/constants";
import { FORMATIONS } from "@/lib/formations";

type OwnedCard = {
  id: string;
  ovr: number;
  position: string;
  tier: string;
  imageUrl: string | null;
  name: string;
  playerId: string;
};

type StarterSlot = {
  slotIndex: number;
  pos: string;
  x: number;
  y: number;
  cardId: string | null;
  card: OwnedCard | null;
  isCaptain: boolean;
  isViceCaptain: boolean;
};

type BenchSlot = {
  slotIndex: number;
  priority: number;
  cardId: string | null;
  card: OwnedCard | null;
};

type PickerSlot = { slotIndex: number; pos?: string; priority?: number };

export default function FantasyPitch({
  gameweekId,
  gameweekNumber,
  deadline,
  locked,
  formation: initialFormation,
  formations,
  starters,
  bench,
  ownedCards,
}: {
  gameweekId: string;
  gameweekNumber: number;
  deadline: string;
  locked: boolean;
  formation: string;
  formations: string[];
  starters: StarterSlot[];
  bench: BenchSlot[];
  ownedCards: OwnedCard[];
}) {
  const router = useRouter();
  const [formation, setFormationState] = useState(initialFormation);
  const [slotCard, setSlotCard] = useState<Record<number, string | null>>(() => {
    const init: Record<number, string | null> = {};
    for (const s of starters) init[s.slotIndex] = s.cardId;
    for (const b of bench) init[b.slotIndex] = b.cardId;
    return init;
  });
  const [captainSlot, setCaptainSlot] = useState<number | null>(
    starters.find((s) => s.isCaptain)?.slotIndex ?? null,
  );
  const [viceSlot, setViceSlot] = useState<number | null>(
    starters.find((s) => s.isViceCaptain)?.slotIndex ?? null,
  );
  const [pickerSlot, setPickerSlot] = useState<PickerSlot | null>(null);
  const [pending, setPending] = useState(false);
  const [error, setError] = useState<string | null>(null);

  const layout = FORMATIONS[formation] ?? FORMATIONS[initialFormation];
  const cardById = new Map(ownedCards.map((c) => [c.id, c]));
  const usedPlayerIds = new Set(
    Object.values(slotCard)
      .filter((id): id is string => !!id)
      .map((id) => cardById.get(id)?.playerId)
      .filter((id): id is string => !!id),
  );

  function pickFormation(f: string) {
    if (f === formation || pending || locked) return;
    setFormationState(f);
    // ตำแหน่งของแต่ละ index (0-10) อาจเปลี่ยนกลุ่มเมื่อสลับ formation — เคลียร์ตัวจริงทั้งหมดกันค่าค้างผิดตำแหน่ง
    setSlotCard((prev) => {
      const next = { ...prev };
      for (let i = 0; i < 11; i++) next[i] = null;
      return next;
    });
    setCaptainSlot(null);
    setViceSlot(null);
  }

  function pick(slotIndex: number, cardId: string | null) {
    setSlotCard((prev) => ({ ...prev, [slotIndex]: cardId }));
    if (cardId === null) {
      if (captainSlot === slotIndex) setCaptainSlot(null);
      if (viceSlot === slotIndex) setViceSlot(null);
    }
    setPickerSlot(null);
  }

  function setCaptain(slotIndex: number) {
    setCaptainSlot(slotIndex);
    if (viceSlot === slotIndex) setViceSlot(null);
  }

  function setVice(slotIndex: number) {
    setViceSlot(slotIndex);
    if (captainSlot === slotIndex) setCaptainSlot(null);
  }

  async function save() {
    setPending(true);
    setError(null);
    const lineup = Object.entries(slotCard)
      .filter(([, cardId]) => cardId)
      .map(([slotIndexStr, cardId]) => {
        const slotIndex = Number(slotIndexStr);
        return {
          cardId: cardId as string,
          slotIndex,
          isCaptain: slotIndex === captainSlot,
          isViceCaptain: slotIndex === viceSlot,
        };
      });
    const result = await saveEntryAction(gameweekId, formation, lineup);
    setPending(false);
    if (!result.ok) {
      setError(result.error ?? "บันทึกไม่สำเร็จ");
      return;
    }
    router.refresh();
  }

  return (
    <div className="px-3 pt-5 pb-24">
      <div className="mb-3 text-center">
        <p className="text-sm font-bold">Gameweek {gameweekNumber}</p>
        <p className="text-xs text-muted">
          {locked ? "ล็อกทีมแล้ว" : `แก้ทีมได้ถึง ${new Date(deadline).toLocaleString("th-TH")}`}
        </p>
      </div>

      <div className="mb-3 flex gap-2 overflow-x-auto pb-1">
        {formations.map((f) => (
          <button
            key={f}
            onClick={() => pickFormation(f)}
            disabled={locked}
            className={`shrink-0 rounded-lg px-3 py-1.5 text-sm font-semibold ${
              f === formation
                ? "bg-primary text-primary-foreground"
                : "border border-border bg-surface text-muted"
            }`}
          >
            {f}
          </button>
        ))}
      </div>

      <div className="relative aspect-[3/4] w-full overflow-hidden rounded-2xl border border-border bg-gradient-to-b from-green-800/60 to-green-900/70">
        <div className="absolute left-1/2 top-1/2 h-24 w-24 -translate-x-1/2 -translate-y-1/2 rounded-full border border-white/15" />
        <div className="absolute left-0 top-1/2 h-px w-full bg-white/15" />

        {layout.map((slot, i) => {
          const cardId = slotCard[i];
          const card = cardId ? cardById.get(cardId) ?? null : null;
          return (
            <button
              key={i}
              onClick={() => !locked && setPickerSlot({ slotIndex: i, pos: slot.pos })}
              disabled={locked}
              className="absolute flex -translate-x-1/2 -translate-y-1/2 flex-col items-center"
              style={{ left: `${slot.x}%`, top: `${slot.y}%`, width: "20%" }}
            >
              {card ? (
                <div className="relative w-full">
                  <PlayerCard imageUrl={card.imageUrl} name={card.name} ovr={card.ovr} position={card.position} />
                  {captainSlot === i && (
                    <span className="absolute -right-1 -top-1 flex h-5 w-5 items-center justify-center rounded-full bg-primary text-[10px] font-bold text-primary-foreground">
                      C
                    </span>
                  )}
                  {viceSlot === i && (
                    <span className="absolute -right-1 -top-1 flex h-5 w-5 items-center justify-center rounded-full bg-accent text-[10px] font-bold text-black">
                      V
                    </span>
                  )}
                </div>
              ) : (
                <div className="flex aspect-square w-full flex-col items-center justify-center rounded-full border-2 border-dashed border-white/30 bg-black/20">
                  <span className="text-[10px] font-bold text-white/70">{slot.pos}</span>
                  <span className="text-lg leading-none text-white/50">+</span>
                </div>
              )}
            </button>
          );
        })}
      </div>

      <p className="mt-3 text-center text-xs text-muted">แตะช่องในสนามเพื่อจัดผู้เล่น</p>

      <div className="mt-4">
        <p className="mb-2 text-xs font-bold text-muted">ตัวสำรอง</p>
        <div className="grid grid-cols-4 gap-2">
          {bench.map((b) => {
            const cardId = slotCard[b.slotIndex];
            const card = cardId ? cardById.get(cardId) ?? null : null;
            return (
              <button
                key={b.slotIndex}
                onClick={() => !locked && setPickerSlot({ slotIndex: b.slotIndex, priority: b.priority })}
                disabled={locked}
                className="relative"
              >
                {card ? (
                  <PlayerCard imageUrl={card.imageUrl} name={card.name} ovr={card.ovr} position={card.position} />
                ) : (
                  <div className="flex aspect-[900/1269] w-full flex-col items-center justify-center rounded-xl border-2 border-dashed border-border bg-surface-2">
                    <span className="text-lg text-muted">+</span>
                  </div>
                )}
                <span className="absolute left-1 top-1 flex h-4 w-4 items-center justify-center rounded-full bg-black/60 text-[9px] font-bold text-white">
                  {b.priority}
                </span>
              </button>
            );
          })}
        </div>
      </div>

      {error && <p className="mt-3 text-center text-sm text-red-400">{error}</p>}

      {!locked && (
        <button
          onClick={save}
          disabled={pending}
          className="mt-4 w-full rounded-xl bg-primary py-3 font-bold text-primary-foreground disabled:opacity-50"
        >
          {pending ? "กำลังบันทึก..." : "บันทึกทีม"}
        </button>
      )}

      {pickerSlot && (
        <div
          className="fixed inset-0 z-50 flex items-end bg-black/70"
          onClick={() => !pending && setPickerSlot(null)}
        >
          <div
            className="mx-auto max-h-[70vh] w-full max-w-md overflow-y-auto rounded-t-2xl border-t border-border bg-surface p-4"
            onClick={(e) => e.stopPropagation()}
          >
            <div className="mb-3 flex items-center justify-between">
              <h3 className="font-bold">
                เลือกผู้เล่น{pickerSlot.pos ? ` ช่อง ${pickerSlot.pos}` : ` ตัวสำรอง ${pickerSlot.priority}`}
              </h3>
              {slotCard[pickerSlot.slotIndex] && (
                <button onClick={() => pick(pickerSlot.slotIndex, null)} className="text-sm text-red-300">
                  เอาออก
                </button>
              )}
            </div>

            {(() => {
              const currentCardId = slotCard[pickerSlot.slotIndex];
              const candidates = ownedCards.filter((c) =>
                pickerSlot.pos
                  ? POSITION_GROUP[c.position as Position] === POSITION_GROUP[pickerSlot.pos as Position]
                  : true,
              );
              if (candidates.length === 0) {
                return <p className="py-6 text-center text-sm text-muted">ไม่มีการ์ดที่ใช้ได้ในช่องนี้</p>;
              }
              return (
                <div className="grid grid-cols-4 gap-2">
                  {candidates.map((c) => {
                    const usedElsewhere = c.id !== currentCardId && usedPlayerIds.has(c.playerId);
                    return (
                      <button
                        key={c.id}
                        onClick={() => !usedElsewhere && pick(pickerSlot.slotIndex, c.id)}
                        disabled={usedElsewhere}
                        className={`relative rounded-lg p-0.5 ${usedElsewhere ? "opacity-30" : ""}`}
                      >
                        <PlayerCard imageUrl={c.imageUrl} name={c.name} ovr={c.ovr} position={c.position} />
                        <span className="mt-0.5 block truncate text-center text-[9px] text-muted">
                          {c.ovr} {c.position}
                        </span>
                      </button>
                    );
                  })}
                </div>
              );
            })()}

            {pickerSlot.pos && slotCard[pickerSlot.slotIndex] && (
              <div className="mt-3 flex gap-2 border-t border-border pt-3">
                <button
                  onClick={() => setCaptain(pickerSlot.slotIndex)}
                  className={`flex-1 rounded-lg py-2 text-sm font-bold ${
                    captainSlot === pickerSlot.slotIndex
                      ? "bg-primary text-primary-foreground"
                      : "border border-border"
                  }`}
                >
                  ตั้งเป็นกัปตัน (C)
                </button>
                <button
                  onClick={() => setVice(pickerSlot.slotIndex)}
                  className={`flex-1 rounded-lg py-2 text-sm font-bold ${
                    viceSlot === pickerSlot.slotIndex ? "bg-accent text-black" : "border border-border"
                  }`}
                >
                  ตั้งเป็นรองกัปตัน (V)
                </button>
              </div>
            )}
          </div>
        </div>
      )}
    </div>
  );
}
```

- [ ] **Step 2: ตรวจ TypeScript ผ่าน**

Run: `npx tsc --noEmit`
Expected: ไม่มี error (แก้ error ที่ค้างจาก Task 7 แล้ว)

- [ ] **Step 3: ตรวจ ESLint ผ่าน**

Run: `npm run lint`
Expected: ไม่มี error

- [ ] **Step 4: Commit**

```bash
git add src/components/FantasyPitch.tsx
git commit -m "feat(fantasy): add FantasyPitch UI — formation/starter/bench/captain (7A)"
```

---

### Task 9: Dev seed script + Browser verification บน Preview

**Files:**
- Create: `verify-fantasy-seed.ts` (ชั่วคราว รันเพื่อสร้าง Gameweek ทดสอบ แล้วลบทิ้ง — ไม่ใช่ admin UI จริง ซึ่งเป็นงานของ 7B)

**Interfaces:**
- Consumes: `prisma` (`src/lib/prisma.ts`)
- Produces: 1 row ใน `Gameweek` ที่ deadline ในอนาคต ให้หน้า `/fantasy` มีข้อมูลให้ทดสอบใน Preview

- [ ] **Step 1: เขียน seed script**

```ts
// verify-fantasy-seed.ts — ชั่วคราว สร้าง Gameweek สำหรับทดสอบใน Preview แล้วลบทิ้ง (ไม่ commit)
import { prisma } from "./src/lib/prisma";

async function main() {
  const existing = await prisma.gameweek.findFirst({ where: { number: 1 } });
  if (existing) {
    console.log("Gameweek 1 มีอยู่แล้ว:", existing);
    return;
  }
  const gw = await prisma.gameweek.create({
    data: {
      number: 1,
      deadline: new Date(Date.now() + 7 * 24 * 3600_000), // 7 วันข้างหน้า
      monthKey: "2026-07",
    },
  });
  console.log("สร้าง Gameweek สำหรับทดสอบแล้ว:", gw);
}

main()
  .catch((e) => {
    console.error(e);
    process.exit(1);
  })
  .finally(() => prisma.$disconnect());
```

- [ ] **Step 2: รัน seed script**

Run: `npx tsx verify-fantasy-seed.ts`
Expected: `สร้าง Gameweek สำหรับทดสอบแล้ว: { ... number: 1 ... }`

- [ ] **Step 3: Browser check ผ่าน Preview**

เปิด Preview → login ด้วยบัญชี `qatester` / `qatest1234` (มีการ์ดครบ 654 ใบ ตาม `.handoffs/157.md`) → ไปที่ `/fantasy`:
- เห็นข้อความ "Gameweek 1" + deadline 7 วันข้างหน้า
- แตะช่องตัวจริงในสนาม → เห็น bottom sheet รายชื่อการ์ดกรองตามตำแหน่งกลุ่มถูกต้อง (เช่น แตะช่อง GK เห็นเฉพาะการ์ด GK)
- เลือกครบ 11 ตัวจริง + 4 ตัวสำรอง
- แตะการ์ดตัวจริง → กดปุ่ม "ตั้งเป็นกัปตัน (C)" เห็น badge "C" บนการ์ด → ตั้งรองกัปตันอีกคน เห็น badge "V"
- ลองสลับ Formation → เห็นตัวจริงถูกเคลียร์ทั้งหมด (ตามที่ตั้งใจไว้กันตำแหน่งค้างผิดกลุ่ม)
- กด "บันทึกทีม" → ไม่มี error แสดง → refresh หน้า → ทีมที่จัดไว้ยังอยู่ครบ (verify snapshot บันทึกจริง)
- ลองใช้การ์ดนักเตะคนเดียวกัน (คนละ tier ถ้ามี) ลงสองช่อง → กด "บันทึกทีม" → ต้องเห็น error "ห้ามใช้นักเตะคนเดียวกันซ้ำในทีม..."

- [ ] **Step 4: ลบ seed script ชั่วคราว**

Run: `rm verify-fantasy-seed.ts`

(ข้อมูล Gameweek ที่สร้างไว้ใน DB ปล่อยไว้ได้ — จะถูกใช้ต่อใน 7B ตอนทำ admin panel หรือ query `prisma.gameweek.deleteMany({where:{number:1}})` ทิ้งเองถ้าต้องการล้าง)

---

### Task 10: อัพเดต `docs/TASKS.md` + verification สุดท้าย

**Files:**
- Modify: `docs/TASKS.md`

- [ ] **Step 1: กา `[x]` ให้ทุกข้อของ 7A ที่เสร็จแล้ว**

เปิด `docs/TASKS.md` หาหัวข้อ `### ขั้น 7A — Core squad + snapshot` เปลี่ยนทุก `- [ ]` เป็น `- [x]` (5 ข้อ: Prisma schema, fantasyConfig, service จัดทีม, snapshot, หน้าจัดทีม, test)

- [ ] **Step 2: รัน verification เต็มชุด**

```bash
npx tsc --noEmit
npm run lint
npm run build
```
Expected: ทั้ง 3 คำสั่งผ่านไม่มี error

- [ ] **Step 3: ตรวจว่าไม่มีไฟล์ทดสอบชั่วคราวหลงเหลือ**

Run: `git status --short`
Expected: ไม่มีไฟล์ `verify-*.ts` ค้างอยู่ (ถูกลบไปหมดแล้วใน task ก่อนหน้า)

- [ ] **Step 4: Commit**

```bash
git add docs/TASKS.md
git commit -m "docs(tasks): mark ขั้น 7A complete (Fantasy core squad + snapshot)"
```

---

## Self-Review

**Spec coverage:**
- หัวข้อ 1 (โครงสร้างทีม, ownership, duplicate-player) → Task 3/5 ✓
- หัวข้อ 2 (snapshot ต่อ Gameweek, deadline immutable, clone) → Task 4/5 ✓
- หัวข้อ 3 (position group ต้องตรง slot, freeze `fantasyPositionGroup`) → Task 1 (schema field) + Task 3 (validate) + Task 5 (freeze ตอน save) ✓
- Data Model (8 model) → Task 1 ✓
- Core Logic ที่ระบุไว้ (`fantasy.ts`) → Task 3/4/5 ✓
- หน้าจัดทีม mobile-first → Task 7/8 ✓
- Edge case #1 (deadline race), #2 (cardId ไม่ได้เป็นเจ้าของ), #3 (playerId ซ้ำ), #9 (ผู้เล่นใหม่การ์ดไม่พอ 15 — ไม่บล็อก) → ครอบคลุมใน Task 3/5 test cases
- หัวข้อ Verification "pure function tests" + "concurrency tests" (บางส่วน — deadline-boundary) → Task 3/5
- **นอกขอบเขต 7A ตามที่ตั้งใจ**: auto-substitution (หัวข้อ 5), captain fallback ตอนคิดคะแนนจริง (หัวข้อ 6 — 7A แค่เก็บ flag ไม่คิดคะแนน), Match/PlayerMatchStat logic, state machine ปิด GW, leaderboard, reward, API sync — ทั้งหมดอยู่ใน 7B-7D ตามที่ระบุไว้ใน `docs/TASKS.md`

**Placeholder scan:** ไม่มี TBD/TODO — ทุก step มีโค้ดสมบูรณ์

**Type consistency:** `LineupInput`/`EnrichedEntry` ประกาศครั้งเดียวใน Task 3 แล้วใช้ชื่อเดิมทุก task ถัดไป (Task 5 `saveEntry`, Task 6 `saveEntryAction`, Task 8 client ประกอบ lineup ตรงกับ shape เดียวกัน) — `getOrCreateEntry` คืน type จาก Prisma โดยตรง (`FantasyEntry & { slots: FantasyEntrySlot[] }`) ใช้ตรงกันทั้ง Task 4 และ Task 7
